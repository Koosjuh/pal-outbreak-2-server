[Service]
# 2026-07-07: lean hot path ON — no per-message capture-log writes.
# Rationale: lean OFF + the ~25/s op48 flood filled the Pi disk (ENOSPC outage 2026-07-07);
# this is also the validated G11 scale config. Rollback = remove this file (and manage disk).
Environment=SNAP_LEAN_HOTPATH=1
