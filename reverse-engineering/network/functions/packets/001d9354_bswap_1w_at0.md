# 0x001d9354 bswap_1w_at0

| field | value |
|---|---|
| Original address | 0x001d9354 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001d9354 |
| Resolved name | bswap_word0 (op0x35 scalar normalizer) |
| Subsystem | packets |
| Relevance | helper |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** FUN_001d9f78 (inbound demux, switch case 0x32 → opcode 0x35)
**Callees:** —
**Referenced globals:** —
**Referenced strings:** —
**Referenced opcodes:** 0x35 (inbound); handler = conn+0x5fc
**State vars:** —

## Behavioral explanation
Leaf endian normalizer for opcode 0x35: byte-swaps the single u32 at the payload start (+0x00). The
payload is a scalar (one 32-bit value); after the swap the demux invokes the `conn+0x5fc` callback.

## Input / output
- `param_1` (uint*): pointer to the payload word. Void.
- Post: `param_1[0]` host-endian.

## Side effects
- In-place single-word mutation. No globals/packets.

## Important branches
- None (straight-line single swap).

## Constants & flags
- Field offset 0x00. Standard 4-byte bswap idiom.

## Corrected reconstruction
```c
// Inbound normalizer, SN@P opcode 0x35: single scalar word.
void bswap_word0(u32 *p)
{
    p[0] = bswap32(p[0]);
}
```

## Evidence
- Raw: `FUN_001d9354.c`.
- Dispatch: `FUN_001d9f78.c` case 0x32 → `FUN_001d9354(param_2+0x3c)` → `conn+0x5fc`. opcode = case+3 = 0x35.
- Runtime-unvalidated.

## Remaining uncertainty
- Opcode 0x35's scalar meaning (id, status code, count?) is not decoded.
