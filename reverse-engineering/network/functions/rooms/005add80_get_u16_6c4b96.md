# 0x005add80 get_selected_room_u16_6c4b96

| field | value |
|---|---|
| Original address | 0x005add80 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_005add80 |
| Resolved name | get_selected_room_u16_6c4b96 |
| Subsystem | rooms |
| Relevance | helper |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** FUN_005f4480
**Callees:** —
**Referenced globals:** `0x6c4b96` u16 field (in the selected-room tuple block `0x6c4b96..0x6c4b9c`)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Trivial leaf accessor: returns the 16-bit value at `0x6c4b96`, a field of the currently-selected-room state
block. Paired in the same caller (FUN_005f4480) with FUN_005adcd0, which reads the sibling fields at
`0x6c4b98/0x6c4b99/0x6c4b9c`. `0x6c4b96` sits just below those; it is the room's `+0x16` u16 attribute
(candidate: current/room-attribute word the list UI reads alongside the id).

## Input / output
- no params.
- returns `undefined2` (u16) : the value at `0x6c4b96`.

## Side effects
- None (pure read).

## Important branches
- None (single return).

## Constants & flags
- None.

## Corrected reconstruction
```c
u16 get_selected_room_u16_6c4b96(void)
{
    return *(u16*)0x6c4b96;   // uRam006c4b96
}
```

## Evidence
- Raw: `return uRam006c4b96;` (12-byte leaf). Adjacency to the FUN_005adcd0 tuple and shared caller
  FUN_005f4480 establish it as part of the selected-room state read. Runtime-unvalidated (field semantics).

## Remaining uncertainty
- The precise meaning of the `0x6c4b96` word (player count? room flags? attribute) is not pinned; only its
  location and that it is read by the room UI are established.

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_005add80.c`  — untouched decompiler output.
