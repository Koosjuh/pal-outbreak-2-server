# 0x005c60d0 send_op11

| field | value |
|---|---|
| Original address | 0x005c60d0 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_005c60d0 |
| Resolved name | send_op11_word |
| Subsystem | rooms |
| Relevance | core |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** FUN_005bb610, FUN_005bbc20
**Callees:** — (memset `func_0x00106b60`; memcpy `func_0x001069a8`; send `func_0x001df380`)
**Referenced globals:** 0x6febac net-ctx
**Referenced strings:** —
**Referenced opcodes:** app 0x11
**State vars:** —

## Behavioral explanation
Emits a reliable **app-opcode 0x11** control message with one 4-byte argument. Same builder as
op0b/op0c: zero-fill 0x304, `body[0]=0x11`, 4-byte arg at +4, send 8 bytes. Reached from two callers —
`FUN_005bbc20` (the room-setup path, right after op0c PREPARE) and `FUN_005bb610`. op11 immediately
follows op0c/op11 in the room-setup handshake, so it is plausibly the PREPARE-ack / commit companion
(op10 is room-info, 0x11 = op10+1). The 4-byte word is a room/slot handle.

## Input / output
- `param_1` (dest): destination (send arg6).
- `param_2` (ptr→u32): 4-byte argument copied to body+4.
- Returns void.

## Side effects
- One reliable app message, opcode 0x11, length 8, channel 1, to `param_1`.

## Important branches
None.

## Constants & flags
- `0x11` — app opcode. `0x8000` — SNAP_FLAG_RELIABLE. length **8**. `1` — channel.

## Corrected reconstruction
```c
enum { OP_11 = 0x11 };
#define SNAP_FLAG_RELIABLE 0x8000

void send_op11_word(void *dest /*param_1*/, const u32 *arg /*param_2*/)
{
    u8 body[0x310];
    memset(body, 0, 0x304);
    body[0] = OP_11;
    memcpy(&body[4], arg, 4);
    snap_send_app(g_net_ctx, SNAP_FLAG_RELIABLE, body, /*len*/8, /*channel*/1, dest);
}
```

## Evidence
- Raw: `auStack_310[0]=0x11`; `func_0x001069a8(auStack_30c,param_2,4)`; send len 8.
- Two callers incl. `FUN_005bbc20` (shared with `send_op0c_prepare`). op11 not in the supplied wire list.
- Runtime-unvalidated.

## Remaining uncertainty
op11's protocol role (PREPARE-ack vs a second room-info variant) — inferred from call adjacency, not
observed traffic.

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_005c60d0.c`  — untouched decompiler output.
