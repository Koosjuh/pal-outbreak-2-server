# 0x005c6150 send_op0e

| field | value |
|---|---|
| Original address | 0x005c6150 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_005c6150 |
| Resolved name | send_op0e_bare |
| Subsystem | rooms |
| Relevance | core |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** FUN_005be330
**Callees:** — (memset `func_0x00106b60`; send `func_0x001df380`)
**Referenced globals:** 0x6febac net-ctx
**Referenced strings:** —
**Referenced opcodes:** app 0x0e
**State vars:** —

## Behavioral explanation
Emits a reliable **app-opcode 0x0e** bare control message — no payload. Zero-fills a 0x304 stack body,
sets `body[0]=0x0e`, and transmits only **4 bytes** (opcode + 3 pad). This is the empty-body member of
the send family; op0e is a pure signal (a room-state toggle / leave-confirm class message), carrying its
meaning entirely in the opcode. Single caller `FUN_005be330`.

## Input / output
- `param_1` (dest): destination (send arg6).
- Returns void.

## Side effects
- One reliable app message, opcode 0x0e, length 4, channel 1, to `param_1`.

## Important branches
None.

## Constants & flags
- `0x0e` — app opcode. `0x8000` — SNAP_FLAG_RELIABLE. length **4** (opcode only). `1` — channel.

## Corrected reconstruction
```c
enum { OP_0E = 0x0e };
#define SNAP_FLAG_RELIABLE 0x8000

void send_op0e_bare(void *dest /*param_1*/)
{
    u8 body[0x310];
    memset(body, 0, 0x304);
    body[0] = OP_0E;
    snap_send_app(g_net_ctx, SNAP_FLAG_RELIABLE, body, /*len*/4, /*channel*/1, dest);
}
```

## Evidence
- Raw: `auStack_310[0]=0xe`; `func_0x001df380(uRam006febac,0x8000,body,4,1,param_1)` (no memcpy).
- Twin of `send_op0f` (0x005c61b0), which differs only in `body[0]=0xf`. op0e not in supplied wire list.
- Runtime-unvalidated.

## Remaining uncertainty
Protocol role of the bare op0e (leave/confirm/toggle) is inferred; not observed on the wire.

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_005c6150.c`  — untouched decompiler output.
