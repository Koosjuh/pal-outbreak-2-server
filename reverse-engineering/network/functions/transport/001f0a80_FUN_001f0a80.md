# 0x001f0a80 snap_rpc_op35_cmd

| field | value |
|---|---|
| Original address | 0x001f0a80 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001f0a80 |
| Resolved name | snap_rpc_op35_cmd |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** FUN_001ef0d0 (thin wrapper, no flag write)
**Callees:** FUN_001ef3e0 (ipc lock), FUN_001ef400 (snap_ipc_call_rpc), FUN_001ef3f0 (ipc unlock)
**Referenced globals:** 0x37aee0 (IPC sema); 0x37cf00 send A; 0x37bf00 recv B (+0x18 status)
**Referenced strings:** —
**Referenced opcodes:** transport fno 0x35
**State vars:** —

## Behavioral explanation
SN@P transport RPC stub for opcode **0x35** — a parameterless command. Header-only request, returns the
status word. Its wrapper `FUN_001ef0d0` adds no side effect, so like op0x53 the domain meaning is not
recoverable here beyond "an argument-free room/session command in the 0x25–0x3a block".

## Input / output
- **Inputs:** none.
- **Returns** (u16): IOP status word from `B+0x18`.

## Side effects
- `WaitSema`/`SignalSema` on `0x37aee0`; overwrites shared buffers A/B; blocks on the RPC.

## Important branches
None.

## Constants & flags
- Transport fno `0x35`. `ssize=0x20`, `rsize=0x20`. Status at `+0x18`.

## Corrected reconstruction
```c
// SN@P transport RPC stub — opcode 0x35: parameterless command. IOP-side meaning unknown.
u16 snap_rpc_op35_cmd(void)
{
    snap_ipc_lock();                                        // WaitSema(0x37aee0)
    snap_ipc_call_rpc(0x35, SEND_A, 0x20, RECV_B, 0x20);
    u16 status = *(u16*)(RECV_B + 0x18);                    // 0x37bf18
    snap_ipc_unlock();                                      // SignalSema(0x37aee0)
    return status;
}
```

## Evidence
- Raw: `FUN_001ef400(0x35,0x37cf00,0x20,0x37bf00,0x20)`, returns `uRam0037bf18`.
- Caller `FUN_001ef0d0`: pure pass-through.
- Confidence **Medium** (mechanism High; opcode meaning unknown). Runtime validated: no.

## Remaining uncertainty
No caller flag/state write to anchor a meaning. Sits between op34 (query) and op37 (2-word cmd) in the
room-op block; role unproven.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001f0a80.c`  — untouched decompiler output.
