# 0x001abf00 FUN_001abf00

| field | value |
|---|---|
| Original address | 0x001abf00 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001abf00 |
| Resolved name | draw_formatted_text [inferred] |
| Subsystem | transport (mis-tagged — text/console rendering) |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** — (not captured)
**Callees:** FUN_0010e778 (vsnprintf-style format: dst-buf, fmt, va_list), FUN_001ac140 (set colour/attr), FUN_001ac390 (set two style bytes/pair), FUN_001ac110 (set draw position x,y), FUN_001ac1a0 (draw the buffered string)
**Referenced globals:** format buffer 0x2a5540, style-pair table 0x245708 (stride 2: byte@0x245708, byte@0x245709)
**Referenced strings:** format string passed as param_5
**Referenced opcodes:** —
**State vars:** shared text buffer 0x2a5540

## Behavioral explanation
Formats and draws a text string — a printf-with-style helper for the on-screen console/debug text layer. It marshals the trailing varargs (`param_6..8`) onto the stack and calls `FUN_0010e778(0x2a5540, param_5, va)` to render `param_5` (format string) into the shared buffer `0x2a5540`. It then programs the text renderer state: colour (`FUN_001ac140(param_4)`), a style pair looked up from table `0x245708` by `param_3` (`FUN_001ac390(tbl[param_3*2], tbl[param_3*2+1])`), position (`FUN_001ac110(param_1, param_2)`), draws the buffer (`FUN_001ac1a0(0x2a5540)`), then restores defaults: style pair `(8,8)` and colour `-1`.

## Input / output
- **param_1, param_2**: draw position (x, y) → `FUN_001ac110`.
- **param_3** (`int`): style index into the `0x245708` pair table.
- **param_4**: colour/attribute → `FUN_001ac140`.
- **param_5**: format string.
- **param_6..8**: format arguments (marshalled as a va_list).
- **returns:** void.

## Side effects
- Overwrites shared text buffer `0x2a5540` with the formatted string.
- Mutates renderer colour/style/position state, then restores style `(8,8)` and colour `-1` (0xFFFFFFFFFFFFFFFF).

## Important branches
- None (straight-line); the only data-dependent step is the `0x245708` table index by `param_3`.

## Constants & flags
- `0x2a5540` — shared format/draw buffer.
- `0x245708` — style-pair table (stride 2).
- restore defaults: style pair `(8,8)`, colour `-1`.

## Corrected reconstruction
```c
// [inferred] renderer state helpers:
//   FUN_001ac140(col)      -> set text colour/attribute
//   FUN_001ac390(a,b)      -> set style pair
//   FUN_001ac110(x,y)      -> set draw position
//   FUN_001ac1a0(buf)      -> draw NUL-terminated string
static const u8 STYLE_PAIRS[] = { /*...*/ };   // at 0x245708, stride 2

void draw_formatted_text(int x, int y, int style_idx, u64 colour,
                         const char *fmt, u64 a0, u64 a1, u64 a2)
{
    u64 va[3] = { a0, a1, a2 };
    FUN_0010e778((char*)0x2a5540, fmt, va);            // format into shared buffer

    FUN_001ac140(colour);                              // set colour
    FUN_001ac390(STYLE_PAIRS[style_idx*2],             // set style pair
                 STYLE_PAIRS[style_idx*2 + 1]);
    FUN_001ac110(x, y);                                // set position
    FUN_001ac1a0((char*)0x2a5540);                     // draw

    FUN_001ac390(8, 8);                                // restore default style
    FUN_001ac140(-1);                                  // restore default colour
}
```

## Evidence
- `FUN_0010e778(buf, fmt, &va)` is the canonical (buffer, format, va_list) shape; the trailing three params are copied to a contiguous stack va_list.
- Save-style/colour → draw → restore pattern; indexed style-pair table `0x245708`. Runtime-unvalidated.

## Notes / uncertainty
- Exact meaning of the colour word and style-pair bytes not decoded; `transport` tag appears incorrect (this is UI/console text).

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001abf00.c`  — untouched decompiler output.
