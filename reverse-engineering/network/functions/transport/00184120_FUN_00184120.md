# 0x00184120 FUN_00184120

| field | value |
|---|---|
| Original address | 0x00184120 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_00184120 |
| Resolved name | queue_28a3d0_post |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** —
**Callees:** FUN_0017ff80 (generic list/queue insert on object `0x28a3d0`)
**Referenced globals:** 0x28a3d0 (queue/list control object)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** list object 0x28a3d0

## Behavioral explanation
One-line adapter that posts `param_1` onto the shared list/queue object at `0x28a3d0` by calling the
generic insert primitive `FUN_0017ff80(list, 0, item)`. The middle argument `0` selects a
position/mode (e.g. insert-at-head or default priority). Paired with `FUN_00184140`
(queue_28a3d0_take) which operates on the same object — together they are the enqueue/dequeue face of
this queue.

## Input / output
- `param_1` — the item/node to insert into queue `0x28a3d0`.
- Returns `void`.

## Side effects
- Mutates the `0x28a3d0` list object (links `param_1` in).

## Important branches
- None (unconditional tail-call).

## Constants & flags
- `0x28a3d0` — the target list/queue control object (shared with 0x00184140).
- `0` — position/mode selector passed to the insert primitive [inferred: head / default].

## Corrected reconstruction
```c
extern list_t g_queue_28a3d0;           // 0x28a3d0

void queue_28a3d0_post(void *item)      // param_1
{
    list_insert(&g_queue_28a3d0, 0, item);   // FUN_0017ff80(0x28a3d0, 0, param_1)
}
```

## Evidence
- Line 5: `FUN_0017ff80(0x28a3d0, 0, param_1)` — three-arg insert on object `0x28a3d0`.
- Complement: 0x00184140 calls `FUN_0017ffa0(0x28a3d0, param_1)` on the same object (the take side).

## Remaining uncertainty
- The internal layout of `0x28a3d0` and the meaning of the `0` selector are not decoded; "post" vs
  a different mutation is inferred from the enqueue/dequeue pairing with 0x00184140.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_00184120.c`  — untouched decompiler output.
