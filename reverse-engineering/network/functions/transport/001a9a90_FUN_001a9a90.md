# 0x001a9a90 FUN_001a9a90

| field | value |
|---|---|
| Original address | 0x001a9a90 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001a9a90 |
| Resolved name | handle_table64_add [inferred] |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** — (not captured)
**Callees:** — (none)
**Referenced globals:** handle table at 0x5a9060 (64 × int, -1 = empty)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** 0x5a9060[64]

## Behavioral explanation
Inserts an integer handle into a 64-entry table at `0x5a9060`, using `-1` as the empty sentinel. Linear-scans for the first free slot, stores `param_1`, and returns; if full (index reaches 0x40) it returns without inserting (silent drop). This is the **add** half of an add/remove pair: `FUN_001a9ae0` is the matching **remove** on the same table.

## Input / output
- **param_1** (`int`): handle/id to insert.
- **returns:** void.

## Side effects
- Writes `param_1` into the first `-1` slot of `0x5a9060`; no-op when full.

## Important branches
- `0x3f < iVar1` → table full → return.
- `*piVar2 == -1` → free slot found → store.

## Constants & flags
- `0x40` (64) — table capacity.
- `-1` — empty-slot sentinel.
- `0x5a9060` — table base (shared with `FUN_001a9ae0`).

## Corrected reconstruction
```c
#define HANDLE_TABLE64      ((int*)0x5a9060)
#define HANDLE_TABLE64_CAP  0x40
#define SLOT_EMPTY  (-1)

void handle_table64_add(int handle)
{
    int  i = 0;
    int *slot = HANDLE_TABLE64;
    while (i <= HANDLE_TABLE64_CAP - 1) {   // i > 63 => full
        if (*slot == SLOT_EMPTY) {
            *slot = handle;
            return;
        }
        i++;
        slot++;
    }
    // full: silently drop
}
```

## Evidence
- Base `0x5a9060`, cap 0x40, sentinel `-1`. `FUN_001a9ae0` scans the same base to remove, confirming the pair.
- Runtime-unvalidated.

## Notes / uncertainty
- Semantic meaning of the 64 handles not established here.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001a9a90.c`  — untouched decompiler output.
