# 0x00186610 FUN_00186610

| field | value |
|---|---|
| Original address | 0x00186610 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_00186610 |
| Resolved name | read_triple_at_0x30 |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** —
**Callees:** — (leaf)
**Referenced globals:** —
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Extracts a three-word (12-byte) field from a source object at offset `0x30` and writes it into the
caller's `param_1` output. Copies `src+0x30`, `src+0x34`, `src+0x38` to `out[0]`, `out[1]`, `out[2]`.
A fixed-shape accessor — e.g. reading an (x,y,z) vector, a 3-word id/coordinate triple, or three
contiguous status words that live at `+0x30` of some record.

## Input / output
- `param_1` (`undefined4*`) — destination, receives 3 words.
- `param_2` (`int`) — source object base; the triple is read from `param_2+0x30 .. +0x38`.
- Returns `void`.

## Side effects
- Writes 12 bytes at `param_1[0..2]`. Read-only on the source.

## Important branches
- None.

## Constants & flags
- Source field offsets `+0x30`, `+0x34`, `+0x38` (three consecutive u32).

## Corrected reconstruction
```c
void read_triple_at_0x30(u32 out[3], const u8 *src)   // param_1, param_2
{
    out[0] = *(u32 *)(src + 0x30);
    out[1] = *(u32 *)(src + 0x34);
    out[2] = *(u32 *)(src + 0x38);
}
```

## Evidence
- Lines 5-7: three word copies from `param_2+0x30/+0x34/+0x38` into `param_1[0/1/2]`. Unambiguous.

## Remaining uncertainty
- The semantic identity of the `+0x30` triple (vector vs id-triple vs status words) depends on the
  source struct, which is not identified here.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_00186610.c`  — untouched decompiler output.
