# 0x001f2728 FUN_001f2728

| field | value |
|---|---|
| Original address | 0x001f2728 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001f2728 |
| Resolved name | iopdev_set_flag4 |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** — (subsystem API)
**Callees:** — (none; pure EE-local write)
**Referenced globals:** 0x37e940 (device count), 0x37e944 (device-table base)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** device table (per-index +0x04)

## Behavioral explanation
**Local-only** setter: writes a boolean-domain field (`0` or `1`) into the device control block at
offset +0x04 for `index`. Bounds-checked against the device count and a value ceiling of 1. Pure EE
RAM update, staged for later use by the RPC/VBLANK path. Corresponds to the `flag4` field that
`iopdev_rpc_init` initializes to 1.

## Input / output
- `param_1` — device index (must be `< g_iopdev_count`).
- `param_2` — value, must be `<= 1` (0 or 1); stored at +0x04.
- **Returns** `u32`: `0` on success, `-0xb` (0xfffffff5) if index OOB, `-0xc` (0xfffffff4) if value > 1.

## Side effects
- `table[index].flag4 = (int)param_2` (+0x04).

## Important branches
- `index >= g_iopdev_count` -> return -0xb.
- `param_2 > 1` -> return -0xc.
- else -> write, return 0.

## Constants & flags
- Field offset +0x04. Value domain `{0,1}`. `-0xb`/`-0xc` = subsystem OOB/invalid-value codes.

## Corrected reconstruction
```c
// Set the per-device boolean flag at +0x04 (local only).
uint32_t iopdev_set_flag4(int index, unsigned long value /*0..1*/) {  // [inferred name]
    if ((int)g_iopdev_count <= index) return 0xfffffff5;  // -0xb  (0x37e940)
    if (value > 1)                     return 0xfffffff4;  // -0xc
    g_iopdev_table[index].flag4 = (int)value;             // index*0x38 + 0x37e944 + 4
    return 0;
}
```

## Evidence
- Raw: `if (iRam0037e940 <= param_1) return 0xfffffff5;` / `if (1 < param_2) return 0xfffffff4;`
  / `*(int*)(param_1*0x38 + iRam0037e944 + 4) = param_2;` / return 0.
- Field +0x04 matches the `flag4` init (=1) in iopdev_rpc_init.
- Runtime-unvalidated.

## Remaining uncertainty
- Meaning of flag4 undetermined. Medium.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001f2728.c`  — untouched decompiler output.
