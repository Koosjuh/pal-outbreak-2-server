# 0x001afc20 FUN_001afc20

| field | value |
|---|---|
| Original address | 0x001afc20 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001afc20 |
| Resolved name | asset_loader_state_reset [inferred] |
| Subsystem | transport (mis-tagged — asset/disc loader) |
| Relevance | core |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** — (not captured)
**Callees:** FUN_00106b60 (memset: dst, value, length)
**Referenced globals:** loader state block at 0x337cc8 (5 bytes; byte0 = stage counter used by FUN_001afc40)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** 0x337cc8 (5-byte staged-loader state)

## Behavioral explanation
Resets the staged asset loader's state by zeroing the 5-byte block at `0x337cc8`. The first byte (`cRam00337cc8`) is the stage counter that `FUN_001afc40` walks 0→4; clearing it restarts the load sequence from stage 0. The remaining 4 bytes are the loader's descriptor/scratch state.

## Input / output
- **param:** none.
- **returns:** void.

## Side effects
- `memset(0x337cc8, 0, 5)` — stage counter and scratch reset to 0.

## Important branches
- None.

## Constants & flags
- `0x337cc8` — loader state base.
- `5` — bytes cleared (1 stage byte + 4 state bytes).

## Corrected reconstruction
```c
void asset_loader_state_reset(void)
{
    FUN_00106b60((void*)0x337cc8, 0, 5);   // reset staged loader (stage=0)
}
```

## Evidence
- Zeroes exactly the 5-byte region whose byte0 `cRam00337cc8` drives the `FUN_001afc40` stage machine (0..4). Runtime-unvalidated.

## Notes / uncertainty
- Roles of the 4 non-stage bytes are inferred as descriptor/scratch, not individually decoded.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001afc20.c`  — untouched decompiler output.
