# 0x00189880 FUN_00189880

| field | value |
|---|---|
| Original address | 0x00189880 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_00189880 |
| Resolved name | set_unit_vec3_wrapper |
| Subsystem | transport (likely MISCLASSIFIED — math/graphics) |
| Relevance | peripheral |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** — (not indexed)
**Callees:** FUN_001898a0(float, float, float)
**Referenced globals:** —
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Thin wrapper that calls `FUN_001898a0` with three immediate `0x3f800000` values — i.e. `(1.0f, 1.0f,
1.0f)`. This is a "set unit / identity triple" helper: it forwards a constant unit 3-vector (scale
XYZ, or an RGB/alpha white, or a unit direction) into the real setter. The `0x3f800000` IEEE-754
constant (== `1.0f`) makes the intent unambiguous even though the raw args are typed as integers by
the decompiler.

This function almost certainly does **not** belong to the SN@P/PAL network transport; it was swept
into the `netstack-decompile` bucket by address proximity. No network globals, opcodes, or record
accessors are touched. Flagged as likely math/graphics.

## Input / output
- **Params:** none (`void`).
- **Return:** none (`void`).

## Side effects
- Whatever `FUN_001898a0` writes given `(1.0, 1.0, 1.0)` — presumably a scale/colour/vector store.
  Nothing in this frame.

## Important branches
- None. Single unconditional call.

## Constants & flags
- `0x3f800000` == `1.0f` (three times) — the unit value for each component.

## Corrected reconstruction
```c
// Forward a unit 3-vector (1,1,1) to the underlying XYZ setter. [inferred: scale/colour]
void set_unit_vec3_wrapper(void)
{
    set_vec3(1.0f, 1.0f, 1.0f);   // FUN_001898a0(0x3f800000, 0x3f800000, 0x3f800000)
}
```

## Evidence
- Raw: `FUN_00189880.c` — body is one call `FUN_001898a0(0x3f800000,0x3f800000,0x3f800000)`.
- `0x3f800000` is the exact bit pattern of IEEE-754 single-precision `1.0`.
- No referenced globals/opcodes/strings ⇒ not a transport routine.
- Runtime-unvalidated.

## Notes / uncertainty
- Exact meaning of the triple (scale vs. colour vs. direction) is unknown without decompiling
  `FUN_001898a0`. Subsystem tag is contested — recommend reclassifying out of `transport`.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_00189880.c`  — untouched decompiler output.
