# 0x0019a010 FUN_0019a010

| field | value |
|---|---|
| Original address | 0x0019a010 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_0019a010 |
| Resolved name | tlv_get_value_tag9 |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** — (not indexed)
**Callees:** FUN_0019a2f0 (get root/store handle), FUN_0019a300(root, 9) (search-only lookup),
FUN_0019a350(root, 9, 0) (find-or-load lookup)
**Referenced globals:** —
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Byte-for-byte the same routine as `FUN_00199f10` but for TLV **tag 9**. Gets the root store, does a
search-only lookup of tag `9`; if already resident returns `0`; otherwise find-or-loads tag `9` and
returns its value word (`+0x04`), or `0` if the load fails. See `FUN_00199f10` for the full branch
discussion (including the "search-hit ⇒ 0" asymmetry, preserved verbatim).

## Input / output
- **Params:** none.
- **Return:** `undefined4` — `0` if tag-9 already resident or unloadable; else `entry->value`
  (`*(u32*)(entry+4)`) of the freshly-loaded tag-9 record.

## Important branches
- `if (FUN_0019a300(root, 9) != 0) return 0;` — already present.
- else `e = FUN_0019a350(root, 9, 0); return e ? *(u32*)(e+4) : 0;`

## Constants & flags
- `9` — TLV tag/entry id.
- `+0x04` — value word.

## Corrected reconstruction
```c
#define TLV_TAG_9 9

u32 tlv_get_value_tag9(void)
{
    store_handle root = tlv_root();                 // FUN_0019a2f0
    if (tlv_find(root, TLV_TAG_9) != NULL)          // FUN_0019a300 — search only
        return 0;
    struct tlv_record *e = tlv_find_or_load(root, TLV_TAG_9, 0); // FUN_0019a350
    return e ? e->value : 0;                         // *(u32*)(e+4)
}
```

## Evidence
- Raw: `FUN_0019a010.c` — identical to `FUN_00199f10.c` with tag `9` substituted for `2`.
- Runtime-unvalidated.

## Notes / uncertainty
- Tag-9's meaning undetermined. Same search-hit-returns-0 caveat as `FUN_00199f10`. Control flow High;
  semantics Low/Medium.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_0019a010.c`  — untouched decompiler output.
