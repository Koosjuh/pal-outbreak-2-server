# 0x001aa580 FUN_001aa580

| field | value |
|---|---|
| Original address | 0x001aa580 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001aa580 |
| Resolved name | zero_init_block_0x640 [inferred] |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** — (not captured)
**Callees:** FUN_001aa510 (memset-style fill: dst, value, length)
**Referenced globals:** —
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Thin wrapper that zero-initialises a 0x640-byte (1600-byte) block at `param_1` by delegating to the fill routine `FUN_001aa510(dst, 0, 0x640)`. Used to reset/allocate a fixed 1600-byte control block before use.

## Input / output
- **param_1** (`void*`/`undefined8`): destination block base.
- **returns:** void.

## Side effects
- Writes 0x640 zero bytes at `param_1`.

## Important branches
- None (straight-line).

## Constants & flags
- `0x640` (1600) — block size.
- fill value `0`.

## Corrected reconstruction
```c
void zero_init_block_0x640(void *block)
{
    FUN_001aa510(block, 0, 0x640);   // memset(block, 0, 1600)
}
```

## Evidence
- Single delegated call `FUN_001aa510(param_1, 0, 0x640)` in the standard (dst, val, len) fill signature.
- Runtime-unvalidated; the 1600-byte structure's identity is not established here.

## Notes / uncertainty
- What the 0x640 block is (a session/context/DNS state block) is unknown from this wrapper alone.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001aa580.c`  — untouched decompiler output.
