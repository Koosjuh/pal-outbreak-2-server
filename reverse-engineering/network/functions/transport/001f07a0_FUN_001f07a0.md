# 0x001f07a0 snap_rpc_op2a_phase5

| field | value |
|---|---|
| Original address | 0x001f07a0 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001f07a0 |
| Resolved name | snap_rpc_op2a_phase5 |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** FUN_001eef50 (wrapper: op2a then sets state var 0x37a288 := 5)
**Callees:** FUN_001ef3e0 (ipc lock), FUN_001ef400 (snap_ipc_call_rpc), FUN_001ef3f0 (ipc unlock)
**Referenced globals:** 0x37aee0 (IPC sema); 0x37cf00 send A; 0x37bf00 recv B (+0x18 status)
**Referenced strings:** —
**Referenced opcodes:** transport fno 0x2a
**State vars:** 0x37a288 (SNAP phase; set to 5 by caller)

## Behavioral explanation
SN@P transport RPC stub for opcode **0x2a** — parameterless command. Its wrapper `FUN_001eef50` advances
the SNAP state-machine phase variable `0x37a288 := 5` after the call, paralleling op0x29
(`snap_rpc_op29_phase2`, FUN_001f06f0) which sets the same var to 2. So op29/op2a are two phase-advancing
transitions of the shared SNAP session state machine.

## Input / output
- **Inputs:** none.
- **Returns** (u16): IOP status word from `B+0x18`.

## Side effects
- `WaitSema`/`SignalSema` on `0x37aee0`; overwrites shared buffers A/B; blocks on the RPC.
- Phase var `0x37a288` set to 5 by the caller wrapper (not here).

## Important branches
None.

## Constants & flags
- Transport fno `0x2a`. `ssize=0x20`, `rsize=0x20`. Status at `+0x18`.
- Phase transition: `0x37a288 → 5`.

## Corrected reconstruction
```c
// SN@P transport RPC stub — opcode 0x2a: parameterless command.
// Wrapper FUN_001eef50 advances g_snap_phase (0x37a288) to 5 afterward.
u16 snap_rpc_op2a_phase5(void)
{
    snap_ipc_lock();                                       // WaitSema(0x37aee0)
    snap_ipc_call_rpc(0x2a, SEND_A, 0x20, RECV_B, 0x20);   // header-only round trip
    u16 status = *(u16*)(RECV_B + 0x18);                   // 0x37bf18
    snap_ipc_unlock();                                     // SignalSema(0x37aee0)
    return status;
}
```

## Evidence
- Raw: `FUN_001ef400(0x2a,0x37cf00,0x20,0x37bf00,0x20)`, returns `uRam0037bf18`.
- Caller `FUN_001eef50`: `uRam0037a288 = 5`.
- GLOBALS.md: `0x37a288` phase var written `:=2` (op29 wrapper) and `:=5` (this wrapper).
- Confidence **Medium** (mechanism High; phase semantics external). Runtime validated: no.

## Remaining uncertainty
The concrete milestone that "phase 5" represents in the SNAP session lifecycle is not derivable from this
binary; only the ordering relationship op29(→2) vs op2a(→5) is evidenced.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001f07a0.c`  — untouched decompiler output.
