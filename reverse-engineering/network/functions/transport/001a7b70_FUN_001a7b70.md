# 0x001a7b70 FUN_001a7b70

| field | value |
|---|---|
| Original address | 0x001a7b70 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001a7b70 |
| Resolved name | draw_reg_write (FUN_001a7b70) |
| Subsystem | transport (draw/GS register write shim) |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** FUN_001a0eb0 (GS/draw setup batch), FUN_001a7a80 (clear-colour write)
**Callees:** FUN_0018c8d0
**Referenced globals:** —
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
A thin pass-through that writes a draw/GS register: it forwards its argument(s) to `FUN_0018c8d0`.
Although the decompiler recovered a single-byte parameter here, every observed call site invokes it
with **two** arguments — a register id and a value (e.g. `FUN_001a7b70(0x14, argb)` in
`FUN_001a7a80`, and `FUN_001a7b70(0x15,0)`, `FUN_001a7b70(0x66,1)` etc. in `FUN_001a0eb0`) — so the
true signature is `(reg_id, value)` and both are handed to `FUN_0018c8d0`. It is the register-set
primitive of the asset/display draw layer.

## Input / output
- `param_1` (u8) — register id (declared); at call sites paired with a second `value` argument.
- (call-site) value — the 32-bit register value.
- Returns `void`.

## Side effects
- Whatever `FUN_0018c8d0` does — programs a draw/GS register (queues a GIF/register write). [inferred]

## Important branches
- None (unconditional delegate).

## Constants & flags
- Register ids seen at call sites: `0x14` (colour, from `FUN_001a7a80`), and the
  `{0x15,1,0x12,0x62,0xc,0x66,2}` batch in `FUN_001a0eb0` (mode/state setup, with `0x66←1`).

## Corrected reconstruction
```c
// Draw/GS register-write shim. True call form is (reg_id, value) despite the 1-arg decompile.
void draw_reg_write(u8 reg_id /*param_1*/ /*, u32 value */)
{
    FUN_0018c8d0(reg_id /*, value */);            // program the register / queue the write
}
```
> The decompiler dropped the second parameter; call sites (`FUN_001a7a80`, `FUN_001a0eb0`) prove a
> `(reg, value)` pair is passed through. Flagged `[inferred]` on the value argument.

## Evidence
- Raw lines 5-7: `FUN_0018c8d0(param_1); return;`.
- Two-argument call sites: `FUN_001a7a80` → `FUN_001a7b70(0x14, packed_argb)`; `FUN_001a0eb0` →
  `FUN_001a7b70(0x15,0)`, `FUN_001a7b70(0x66,1)`, etc. Runtime-unvalidated.

## Remaining uncertainty
- The exact behaviour of `FUN_0018c8d0` (immediate register poke vs GIF-packet queue) and the
  register-id namespace.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001a7b70.c`  — untouched decompiler output.
