# 0x001aa780 FUN_001aa780

| field | value |
|---|---|
| Original address | 0x001aa780 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001aa780 |
| Resolved name | reset_state_and_clear_flag_4fa64f [inferred] |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** — (not captured)
**Callees:** FUN_001aa6c0 (sub-reset, called with argument 0)
**Referenced globals:** uRam004fa64f (byte flag)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** 0x4fa64f

## Behavioral explanation
Small reset/teardown helper: invokes `FUN_001aa6c0(0)` (a parameterised reset of a subsystem, arg 0 selecting the "clear/all" case) and then clears the byte flag `uRam004fa64f` to 0. Sequences a subsystem reset followed by clearing an associated status/active flag.

## Input / output
- **param:** none.
- **returns:** void.

## Side effects
- Runs `FUN_001aa6c0(0)`.
- `uRam004fa64f = 0`.

## Important branches
- None.

## Constants & flags
- `0` — argument selecting `FUN_001aa6c0`'s reset mode; also the cleared flag value.

## Corrected reconstruction
```c
void reset_state_and_clear_flag_4fa64f(void)
{
    FUN_001aa6c0(0);       // subsystem reset (mode 0)
    uRam004fa64f = 0;      // clear associated flag
}
```

## Evidence
- Two-statement body: delegated reset + flag clear. Runtime-unvalidated.

## Notes / uncertainty
- Meaning of `FUN_001aa6c0`'s mode argument and of flag `0x4fa64f` not traced.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001aa780.c`  — untouched decompiler output.
