# 0x001f0230 FUN_001f0230

| field | value |
|---|---|
| Original address | 0x001f0230 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001f0230 |
| Resolved name | snap_rpc_cmd1a_set_u16 (SN@P RPC selector 0x1a) [inferred] |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callees:** FUN_001ef3e0 (WaitSema), FUN_001ef400 (sceSifCallRpc), FUN_001ef3f0 (SignalSema).
**Referenced globals:** RPC sema `0x37aee0`; req block `0x37cf00` (arg @ `0x37cf1c`); reply `0x37bf00`
(status @ `0x37bf18`).

## Behavioral explanation
SN@P IOP-module RPC stub, **selector `0x1a`**, single 16-bit argument. Marshals `param_1` into the
request block at `0x37cf1c`, performs the synchronous SIF RPC (send `0x22` bytes, receive `0x20`),
and returns the module status word (`0x37bf18`). No reply payload beyond status ⇒ this is a "command
with one short argument" (a setter/selector into the SN@P library — e.g. select room/mode/index by id).

## Input / output
- **param_1** (`u16`): the argument (id/index/mode) placed at req `+0x1c`.
- **Return** (`u16`): SN@P status word (`>=0` success convention, `<0`/`0xffff` error). [inferred sign]

## Side effects
- WaitSema/SignalSema bracket.
- Writes `uRam0037cf1c = param_1`; issues RPC `0x1a`.

## Important branches
None (straight-line).

## Constants & flags
- `0x1a` = RPC selector. Send len `0x22`, recv len `0x20`.
- Arg offset `0x37cf1c` (req+0x1c); status `0x37bf18` (reply+0x18).

## Corrected reconstruction
```c
u16 snap_rpc_cmd1a_set_u16(u16 arg)
{
    snap_rpc_lock();                                       // FUN_001ef3e0
    *(u16*)0x37cf1c = arg;                                 // request arg
    snap_rpc_call(0x1a, 0x37cf00, 0x22, 0x37bf00, 0x20);  // FUN_001ef400 (sceSifCallRpc)
    u16 status = *(u16*)0x37bf18;                          // reply status
    snap_rpc_unlock();                                     // FUN_001ef3f0
    return status;
}
```

## Evidence
- Raw sets `uRam0037cf1c = param_1`, calls `FUN_001ef400(0x1a,0x37cf00,0x22,0x37bf00,0x20)`, returns
  `uRam0037bf18`. Lock/RPC/unlock helpers confirmed. Runtime-unvalidated.

## Notes / uncertainty
Exact domain command behind selector `0x1a` unknown without runtime; mechanism and marshalling exact.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001f0230.c`  — untouched decompiler output.
