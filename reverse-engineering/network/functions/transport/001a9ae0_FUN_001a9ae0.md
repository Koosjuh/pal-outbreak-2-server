# 0x001a9ae0 FUN_001a9ae0

| field | value |
|---|---|
| Original address | 0x001a9ae0 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001a9ae0 |
| Resolved name | handle_table64_remove [inferred] |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** — (not captured)
**Callees:** — (none)
**Referenced globals:** handle table at 0x5a9060 (64 × int, -1 = empty)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** 0x5a9060[64]

## Behavioral explanation
The **remove** counterpart to `FUN_001a9a90`. Scans all 64 slots of the table at `0x5a9060` and sets every slot equal to `param_1` back to the empty sentinel `-1`. Unlike the add, it does **not** early-exit: it walks the full table, so duplicate registrations of the same handle are all cleared.

## Input / output
- **param_1** (`int`): handle/id to remove.
- **returns:** void.

## Side effects
- Every slot in `0x5a9060` holding `param_1` is reset to `-1`.

## Important branches
- `*piVar2 == param_1` → matching slot → reset to `-1` (continues scanning; no break).
- `iVar1 < 0x40` — full-table scan bound.

## Constants & flags
- `0x40` (64) — table size.
- `-1` — empty sentinel written on removal.
- `0x5a9060` — table base (shared with `FUN_001a9a90`).

## Corrected reconstruction
```c
#define HANDLE_TABLE64      ((int*)0x5a9060)
#define HANDLE_TABLE64_CAP  0x40
#define SLOT_EMPTY  (-1)

void handle_table64_remove(int handle)
{
    int *slot = HANDLE_TABLE64;
    for (int i = 0; i < HANDLE_TABLE64_CAP; i++, slot++) {
        if (*slot == handle)
            *slot = SLOT_EMPTY;      // no break: clears all duplicates
    }
}
```

## Evidence
- Same base/cap/sentinel as `FUN_001a9a90` (add). Full-scan `do/while` to `0x40`.
- Runtime-unvalidated.

## Notes / uncertainty
- Whether duplicate handles ever legitimately occur is unknown; the full scan tolerates them.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001a9ae0.c`  — untouched decompiler output.
