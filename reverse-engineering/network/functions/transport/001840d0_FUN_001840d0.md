# 0x001840d0 FUN_001840d0

| field | value |
|---|---|
| Original address | 0x001840d0 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001840d0 |
| Resolved name | memcpy_bytes |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** FUN_001801b0 (request serialiser; header + payload copies), others
**Callees:** — (leaf)
**Referenced globals:** —
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Byte-granular `memcpy`: copies `param_3` bytes from `param_2` to `param_1`, ascending, one byte per
iteration. No overlap handling, no alignment fast-path — a plain forward copy. Used by the request
serialiser (0x001801b0) to lay the fixed header and variable payloads into the message buffer.

## Input / output
- `param_1` (`undefined1*`) — destination.
- `param_2` (`undefined1*`) — source.
- `param_3` (`int`) — byte count. If `<= 0`, copies nothing.
- Returns `void`.

## Side effects
- Writes `param_3` bytes at `param_1`. Forward copy: unsafe if `dst`/`src` overlap with `dst > src`.

## Important branches
- Loop guard `iVar1 < param_3`; a non-positive count is a no-op.

## Constants & flags
- None.

## Corrected reconstruction
```c
void memcpy_bytes(u8 *dst, const u8 *src, int n)   // param_1, param_2, param_3
{
    for (int i = 0; i < n; i++)
        dst[i] = src[i];
}
```

## Evidence
- Lines 7-12: single ascending byte-copy loop bounded by `param_3`. Unambiguous.

## Remaining uncertainty
- None. (High confidence; only "runtime validated" flag withheld since no trace was captured.)

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001840d0.c`  — untouched decompiler output.
