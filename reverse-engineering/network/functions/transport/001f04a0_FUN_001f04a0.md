# 0x001f04a0 FUN_001f04a0

| field | value |
|---|---|
| Original address | 0x001f04a0 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001f04a0 |
| Resolved name | snap_rpc_get_status_block (SN@P RPC selector 0x23) [inferred] |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callees:** FUN_001ef3e0 (WaitSema), FUN_001ef400 (sceSifCallRpc), FUN_001ef3f0 (SignalSema).
**Callers:** FUN_001eeb50 (link/session-status reader, itself called by `FUN_001bf2e0`).
**Referenced globals:** RPC sema `0x37aee0`; req `0x37cf00`; reply `0x37bf00` (status `0x37bf18`,
data `0x37bf1c..0x37bf44`).

## Behavioral explanation
SN@P IOP-module RPC stub, **selector `0x23`**, no request args, returning a **0x2c-byte data block**
(11 words) from the reply. It runs the synchronous RPC (`0x20` send / `0x4c` recv) and copies reply
words `0x37bf1c..0x37bf44` into the caller's array `param_1[0..10]`, returning the status word. This is
a **status/info query**: `FUN_001eeb50` interprets the first words as a session/link record (two
enum-clamped fields, 0–9 and 0–13) used by `FUN_001bf2e0` for link/adaptor status. The 11-word block
therefore carries connection/session state (ids + two classified state enums + a scaled metric).

## Input / output
- **param_1** (`u32*` out, ≥11 words): receives reply words `+0x1c..+0x44` (0x2c bytes).
- **Return** (`u16`): SN@P status word (`0x37bf18`).

### Reply block mapping (`0x37bf1c` base → param_1[])
| dst | reply off | note |
|---|---|---|
| param_1[0] | 0x37bf1c | word 0 |
| param_1[1] | 0x37bf20 | word 1 |
| param_1[2] | 0x37bf24 | word 2 |
| param_1[3] | 0x37bf28 | word 3 |
| param_1[4] | 0x37bf2c | word 4 |
| param_1[5] | 0x37bf30 | word 5 |
| param_1[6] | 0x37bf34 | word 6 |
| param_1[7] | 0x37bf38 | word 7 |
| param_1[8] | 0x37bf3c | word 8 |
| param_1[9] | 0x37bf40 | word 9 |
| param_1[10]| 0x37bf44 | word 10 |

## Side effects
WaitSema/SignalSema bracket; issues RPC `0x23`; writes 11 words to `param_1`.

## Important branches
None (unconditional copy of all 11 words regardless of status — caller `FUN_001eeb50` gates on
`status >= 0`).

## Constants & flags
- `0x23` = RPC selector (status/info query). Send `0x20`, recv `0x4c`. Status @ `0x37bf18`.
- Consumer `FUN_001eeb50` clamps reply half-words to enums: field A ∈ [0,9] (default 8),
  field B ∈ [0,13] (default 0xb) — likely link-state and session-phase classifications.

## Corrected reconstruction
```c
// SN@P status/info query — RPC selector 0x23, returns an 11-word (0x2c) info block.
u16 snap_rpc_get_status_block(u32 out[11])
{
    snap_rpc_lock();                                       // FUN_001ef3e0
    snap_rpc_call(0x23, 0x37cf00, 0x20, 0x37bf00, 0x4c);  // FUN_001ef400
    const u32 *r = (const u32*)0x37bf1c;                  // reply payload
    for (int i = 0; i < 11; i++) out[i] = r[i];           // 0x2c bytes
    u16 status = *(u16*)0x37bf18;
    snap_rpc_unlock();                                     // FUN_001ef3f0
    return status;
}
```

## Evidence
- Raw copies `uRam0037bf1c..uRam0037bf44` (11 words) to `param_1[0..10]`, returns `uRam0037bf18`;
  `FUN_001ef400(0x23,0x37cf00,0x20,0x37bf00,0x4c)`. Helpers confirmed.
- Consumer `FUN_001eeb50` decodes fields as enums; `FUN_001bf2e0` uses the result for link status.
  Runtime-unvalidated.

## Notes / uncertainty
Field-by-field meaning of the 11 words is only partially known (via `FUN_001eeb50`'s two enum fields
and the `/100` metric in `FUN_001bf2e0`). Marshalling/offsets exact.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001f04a0.c`  — untouched decompiler output.
