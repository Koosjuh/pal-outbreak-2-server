# 0x001ab460 FUN_001ab460

| field | value |
|---|---|
| Original address | 0x001ab460 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001ab460 |
| Resolved name | record_pool_reserve [inferred] |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** — (not captured)
**Callees:** FUN_001ac0b0 (fatal/assert with message ptr), FUN_001ab3d0 (existing-allocation lookup by (param_1,param_2))
**Referenced globals:** cRam00343573 (one-time init flag), sRam002a4a0c (allocation count, max 100), sRam002a4a08 (arena capacity in records), sRam002a4a0a (records used), iRam002a4a10 (arena base), ptr table 0x2a4a1c[100], size table 0x2a4bac[100] (short), type table 0x2a4c74[100] (byte)
**Referenced strings:** 0x245680 (init/assert msg), 0x2456a0 (overflow assert msg)
**Referenced opcodes:** —
**State vars:** the parallel-array record pool at 0x2a4a1c / 0x2a4bac / 0x2a4c74 over arena iRam002a4a10

## Behavioral explanation
Reserves a contiguous run of **`param_1` fixed-size records** (stride 0x98 = 152 bytes) from a linear arena and registers the run in three parallel tables indexed by allocation slot. On first use (`cRam00343573 == 0`) it triggers `FUN_001ac0b0(0x245680)` — a one-time init/assert. It first calls `FUN_001ab3d0(param_1, param_2)` to check for an already-existing matching allocation; if that returns non-zero the reservation is skipped (idempotent / dedup). Otherwise it bounds-checks the pool (≤100 slots) and the arena (must have `param_1` free records), then carves the run:
- record byte offset = `used_records * 0x98` from arena base `iRam002a4a10` (0x98 = `(used*0x12 + used)*8` = `used*19*8`),
- stores that pointer in slot `count` of the ptr table,
- stores `param_1` (the run length in records) in the size table,
- stores `param_2 + 1` as the slot's type tag,
- advances `used += param_1` and `count += 1`.

## Input / output
- **param_1** (`int`): number of 152-byte records to reserve.
- **param_2** (`int`/tag): allocation type/owner; stored as `param_2 + 1` (so 0 is reserved as "empty").
- **returns:** void (result registered in the pool; failure paths abort via `FUN_001ac0b0`).

## Side effects
- Appends one entry to the ptr/size/type tables and advances `count`/`used`.
- May abort the program via `FUN_001ac0b0` on pool-full or arena-out-of-space.

## Important branches
- `cRam00343573 == '\0'` → `FUN_001ac0b0(0x245680)` (first-time init or assert).
- `FUN_001ab3d0(param_1,param_2) != 0` → allocation already exists → return without reserving.
- `sRam002a4a0c == 100` → pool full → `FUN_001ac0b0(0x2456a0)` (abort).
- `(sRam002a4a08 - sRam002a4a0a) < param_1` → not enough free records in arena → `FUN_001ac0b0(0x2456a0)` (abort).

## Constants & flags
- record stride `0x98` (152 bytes) = `(n*0x12 + n)*8`.
- pool slot cap `100`.
- type tag stored as `param_2 + 1` (0 = empty slot).
- `0x2a4a1c` ptr table, `0x2a4bac` size table (short), `0x2a4c74` type table (byte).

## Corrected reconstruction
```c
#define POOL_CAP        100
#define REC_STRIDE      0x98        // 152 bytes = (n*0x12 + n)*8

extern char  pool_inited;           // cRam00343573
extern short pool_count;            // sRam002a4a0c  (slots used)
extern short arena_cap;             // sRam002a4a08  (records total)
extern short arena_used;            // sRam002a4a0a  (records consumed)
extern int   arena_base;            // iRam002a4a10
static int  *pool_ptr  = (int*) 0x2a4a1c;   // [100] record-run base ptr
static short*pool_size = (short*)0x2a4bac;   // [100] run length in records
static char *pool_type = (char*) 0x2a4c74;   // [100] type tag (+1)

void record_pool_reserve(int nrec, int type)
{
    if (!pool_inited)
        FUN_001ac0b0(MSG_POOL_INIT /*0x245680*/);      // one-time init / assert

    if (FUN_001ab3d0(nrec, type) != 0)
        return;                                         // already reserved (dedup)

    if (pool_count == POOL_CAP)
        FUN_001ac0b0(MSG_POOL_FULL /*0x2456a0*/);       // abort
    if (arena_cap - arena_used < nrec)
        FUN_001ac0b0(MSG_POOL_FULL /*0x2456a0*/);       // abort: arena exhausted

    int slot = pool_count;
    pool_ptr[slot]  = arena_base + arena_used * REC_STRIDE;
    pool_size[slot] = (short)nrec;
    pool_type[slot] = (char)type + 1;
    arena_used += (short)nrec;
    pool_count += 1;
}
```

## Evidence
- `(sRam002a4a0a * 0x12 + sRam002a4a0a) * 8` reduces to `used*152` → 152-byte record stride.
- Parallel-array registration (ptr/size/type) with a 100-slot cap and two `FUN_001ac0b0` abort paths keyed to distinct message pointers (0x245680 vs 0x2456a0).
- `FUN_001ab580` frees a run by matching the ptr in `0x2a4a1c`, confirming the ptr-table role. Runtime-unvalidated.

## Notes / uncertainty
- Whether these 152-byte records are the SN@P member/room records or an unrelated arena is not confirmed; do not conflate with the reconstructed `member_record`/`room_record` without further evidence.
- `FUN_001ac0b0` is assumed non-returning (assert/panic); if it returns, execution falls through to the carve with an overflowing arena.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001ab460.c`  — untouched decompiler output.
