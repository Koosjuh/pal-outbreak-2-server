# 0x00188a30 FUN_00188a30

| field | value |
|---|---|
| Original address | 0x00188a30 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_00188a30 |
| Resolved name | clone_blob_at_p0x14_len0x18 |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** —
**Callees:** FUN_00184170 (aligned alloc), thunk_FUN_00197ad0 (handle→ptr)
**Referenced globals:** —
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Sibling of 0x001889b0: duplicates a variable-length blob referenced by `*(param_1+0x14)`, whose byte
length is stored at its offset `+0x18` (i.e. `blob[6]` as words). It allocates a new buffer of that
size (alignment class 4), resolves the handle, and copies the blob **word-by-word** (`len/4`
u32 iterations). Returns the allocation **handle**. The word-granular copy implies this blob is
guaranteed 4-byte-multiple length and 4-byte aligned (vs the byte-copy variant in 0x001889b0).

## Input / output
- `param_1` (`int`) — container whose word at `+0x14` points to the source blob.
- Source blob: `len = *(u32 *)(blob + 0x18)` (== `blob[6]`); copied as `len >> 2` words from base.
- Returns `undefined8` — new buffer's allocation handle.

## Side effects
- Allocates a new buffer (FUN_00184170, class 4); copies `len` bytes (as words) into it.

## Important branches
- None explicit. Word loop bounded by `len >> 2`; a `len` not a multiple of 4 would drop the tail
  bytes (see uncertainty).

## Constants & flags
- Blob pointer at container `+0x14`; length field at blob `+0x18`.
- Alloc alignment class `4`.
- Word-granular copy: `count = len / 4`.

## Corrected reconstruction
```c
// Deep-copy the blob at *(param_1+0x14), length at blob+0x18, word-granular. Returns alloc handle.
u64 clone_blob_at_p0x14_len0x18(container_t *c)   // param_1
{
    u32 *blob = *(u32 **)((u8 *)c + 0x14);        // *(param_1+0x14)
    u32  len  = blob[6];                          // *(blob+0x18)

    u64  hnd  = aligned_alloc_cls(len, 4);        // FUN_00184170(len, 4)
    u32 *dst  = handle_to_ptr(hnd);               // thunk_FUN_00197ad0

    for (u32 i = 0; i < (len >> 2); i++)          // word copy
        dst[i] = blob[i];

    return hnd;                                   // handle, not pointer
}
```

## Evidence
- Lines 11-12: `puVar1 = *(param_1+0x14)`; `len = puVar1[6]` (== `+0x18`); alloc `FUN_00184170(len,4)`.
- Lines 13-19: resolve via `thunk_FUN_00197ad0`, copy `len>>2` words from `puVar1`.
- Line 20: returns handle `uVar3`.

## Remaining uncertainty
- Non-multiple-of-4 lengths would truncate (word loop drops the remainder) — implies a length
  invariant not proven here. Container layout and how it differs from the `+8/+0xc` blob of
  0x001889b0 (two distinct sub-records within one container?) is not decoded.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_00188a30.c`  — untouched decompiler output.
