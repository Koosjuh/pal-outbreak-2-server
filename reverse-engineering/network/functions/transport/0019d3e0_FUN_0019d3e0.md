# 0x0019d3e0 FUN_0019d3e0

| field | value |
|---|---|
| Original address | 0x0019d3e0 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_0019d3e0 |
| Resolved name | netstack_is_ready (FUN_0019d3e0) |
| Subsystem | transport (netstack readiness) |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** — (not captured in this set)
**Callees:** FUN_0017ee60 (stage-1 status query), FUN_0017e550 (stage-2 status query, arg 0)
**Referenced globals:** —
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
A two-stage readiness predicate. It returns `true` only when the first subsystem status query
`FUN_0017ee60()` reports exactly `1` **and** the second query `FUN_0017e550(0)` also reports exactly
`1`. If the first stage is not `1`, the second is never consulted (short-circuit) and it returns
`false`. Typical use is a "both lower layers are up / linked" gate polled before issuing a network
operation.

## Input / output
- No parameters.
- Returns `bool`: `true` iff `FUN_0017ee60() == 1 && FUN_0017e550(0) == 1`, else `false`.

## Side effects
- None (pure query; the callees are status reads).

## Important branches
- `FUN_0017ee60() != 1` → `false` (stage-2 skipped).
- `FUN_0017ee60() == 1` → evaluate `FUN_0017e550(0)`; result is `(that == 1)`.

## Constants & flags
- `1` — the "ready/up" sentinel returned by both stage queries. Any other value = not ready.
- `0` — argument to `FUN_0017e550` (a channel/index selector; [inferred] "primary" instance).

## Corrected reconstruction
```c
// True only when both netstack status stages report state == 1 (ready/linked).
bool netstack_is_ready(void)
{
    if (FUN_0017ee60() != 1)          // stage 1 must be up
        return false;
    return FUN_0017e550(0) == 1;      // ...and stage 2 (instance 0) up
}
```

## Evidence
- Raw lines 8-16: `lVar2 = FUN_0017ee60(); if (lVar2==1){ lVar2=FUN_0017e550(0); bVar1 = lVar2==1; }
  else bVar1=false;`.
- Both callees are outside this decompile set; classified as status queries from the `== 1` compares
  and the gate-before-op shape. Runtime-unvalidated.

## Remaining uncertainty
- Which concrete subsystems `FUN_0017ee60`/`FUN_0017e550` report on (link up? DHCP/DNS resolved?
  IOP module started?) and the meaning of the `0` selector.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_0019d3e0.c`  — untouched decompiler output.
