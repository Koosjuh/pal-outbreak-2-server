# 0x001a0df0 FUN_001a0df0

| field | value |
|---|---|
| Original address | 0x001a0df0 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001a0df0 |
| Resolved name | asset_request_chanA (FUN_001a0df0) |
| Subsystem | transport (asset/RPC request channel) |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** FUN_001a0eb0, FUN_001a1750 (asset loaders — both use the return as a byte length `> 0`)
**Callees:** FUN_001a0bf0 (pre-step keyed by `uRam0034357a`), FUN_001a08d0 (core request/transfer on channel handle `uRam0029bc20`)
**Referenced globals:** `uRam0034357a` (pre-step key/id), `uRam0029bc20` (channel-A handle)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Thin request wrapper for **channel A** (`uRam0029bc20`). It runs a preparation step
`FUN_001a0bf0(uRam0034357a)` (arm/select the request context using a global id) and then issues the
actual request/transfer via `FUN_001a08d0(uRam0029bc20, request, dest, 0)`. `request` (`param_1`) is
the request descriptor/string built by the caller; `dest` (`param_2`) is the reply/output buffer
(callers pass `iRam003435bc`, the work base). Although the decompiler typed this `void`, both callers
consume its value as a `long` reply length and branch on `> 0`, so it returns the byte count produced
by `FUN_001a08d0`.

`FUN_001a0e90` is the identical wrapper for **channel B** (`uRam0029bc18`) minus the `FUN_001a0bf0`
pre-step.

## Input / output
- `param_1` (ptr) — request descriptor/string (e.g. a formatted asset request from `FUN_00109728`).
- `param_2` (ptr) — destination/work buffer for the reply (typically `iRam003435bc`).
- Returns (long, lost as `void` by the decompiler) — reply length from `FUN_001a08d0`; callers treat
  `> 0` as success.

## Side effects
- `FUN_001a0bf0(uRam0034357a)` mutates the shared request context (selects/arms it).
- `FUN_001a08d0` performs the transfer on channel-A handle `uRam0029bc20` and writes into `param_2`.

## Important branches
- None internally (straight-line). Control flow lives in the callees.

## Constants & flags
- `uRam0029bc20` — channel-A handle (vs `uRam0029bc18` = channel-B in `FUN_001a0e90`).
- `uRam0034357a` — global id/selector consumed by the `FUN_001a0bf0` pre-step. [inferred] current
  section/area id.
- trailing `0` arg to `FUN_001a08d0` — flags/mode (0 = default/blocking). [inferred]

## Corrected reconstruction
```c
// Channel-A asset/RPC request: arm the context, then transfer. Returns reply byte length.
long asset_request_chanA(void *request /*param_1*/, void *dest /*param_2*/)
{
    FUN_001a0bf0(uRam0034357a);                       // pre-step: select/arm request ctx
    return FUN_001a08d0(uRam0029bc20, request, dest, 0); // core transfer on channel A
}
```

## Evidence
- Raw lines 5-7: `FUN_001a0bf0(uRam0034357a); FUN_001a08d0(uRam0029bc20,param_1,param_2,0);`.
- Return-value fact: callers `FUN_001a0eb0` (`lVar2 = FUN_001a0df0(...); if (0 < lVar2)`) and
  `FUN_001a1750` (`lVar4 = FUN_001a0df0(...); if (0 < lVar4)`) prove a meaningful `long` return.
- Channel-A vs B distinction from the `uRam0029bc20`/`uRam0029bc18` pair. Unvalidated at runtime.

## Remaining uncertainty
- Exact role of `FUN_001a08d0` (SIF RPC bind/call vs a socket send) and what channels A/B map to.
- Whether `uRam0034357a` is truly the area/section id or another selector.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001a0df0.c`  — untouched decompiler output.
