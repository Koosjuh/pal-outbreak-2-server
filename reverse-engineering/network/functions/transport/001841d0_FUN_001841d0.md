# 0x001841d0 FUN_001841d0

| field | value |
|---|---|
| Original address | 0x001841d0 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001841d0 |
| Resolved name | netstack_teardown_stage2 |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** —
**Callees:** FUN_00183be0, FUN_00197af0, FUN_00182520
**Referenced globals:** uRam0027e580 (subsystem run-state word)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** uRam0027e580

## Behavioral explanation
A shutdown/state-transition step. It sets the subsystem run-state word `uRam0027e580 = 2` (a
"stopping"/stage-2 state) and then runs three teardown routines in order: `FUN_00183be0`,
`FUN_00197af0`, `FUN_00182520`. The state is set *before* the calls so any concurrent consumer sees
"no longer running" while the tear-down executes. No parameters, no return — a fixed epilogue for the
netstack module.

## Input / output
- None. Returns `void`.

## Side effects
- `uRam0027e580 = 2` (run-state advanced to stage 2 / stopping).
- Cascades three subsystem teardowns (FUN_00183be0 → FUN_00197af0 → FUN_00182520).

## Important branches
- None (straight-line).

## Constants & flags
- `2` — value written to `uRam0027e580`; a run-state/phase enum (2 = stopping/teardown stage) whose
  other values are set elsewhere (e.g. running=1). [inferred meaning]

## Corrected reconstruction
```c
extern u32 g_netstack_state;            // uRam0027e580

void netstack_teardown_stage2(void)
{
    g_netstack_state = 2;               // mark stopping/stage-2 before tearing down
    FUN_00183be0();                     // subsystem shutdown A
    FUN_00197af0();                     // subsystem shutdown B
    FUN_00182520();                     // subsystem shutdown C
}
```

## Evidence
- Lines 5-8: `uRam0027e580 = 2;` then three unconditional calls in sequence.

## Remaining uncertainty
- The precise role of each of the three teardown routines and the full state enum for
  `uRam0027e580` are not decoded; "teardown stage 2" is inferred from the state write + shutdown
  call shape.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001841d0.c`  — untouched decompiler output.
