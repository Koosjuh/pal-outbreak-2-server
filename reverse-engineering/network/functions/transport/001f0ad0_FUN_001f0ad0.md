# 0x001f0ad0 snap_rpc_op37_cmd2w

| field | value |
|---|---|
| Original address | 0x001f0ad0 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001f0ad0 |
| Resolved name | snap_rpc_op37_cmd2w |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** FUN_001ef190(ptr) → op37(*ptr) (second word left as junk)
**Callees:** FUN_001ef3e0 (ipc lock), FUN_001ef400 (snap_ipc_call_rpc), FUN_001ef3f0 (ipc unlock)
**Referenced globals:** 0x37aee0 (IPC sema); 0x37cf00 send A (+0x1c u32, +0x20 u32); 0x37bf00 recv B (+0x18 status)
**Referenced strings:** —
**Referenced opcodes:** transport fno 0x37
**State vars:** —

## Behavioral explanation
SN@P transport RPC stub for opcode **0x37** — a command taking two 32-bit input words (`ssize=0x28`),
structurally identical to op0x33 but a different opcode. Stages `param_1` at `A+0x1c` and `param_2` at
`A+0x20`, returns the status. The sole caller supplies only the first word (dereferenced from a pointer)
and leaves the second word as whatever is in the register — so effectively a **one-word command** at the
known call site.

## Input / output
- **param_1** (u32): staged at `A+0x1c` (`uRam0037cf1c`). Caller passes `*ptr`.
- **param_2** (u32): staged at `A+0x20` (`uRam0037cf20`). Caller passes nothing meaningful.
- **Returns** (u16): IOP status word from `B+0x18`.

## Side effects
- `WaitSema`/`SignalSema` on `0x37aee0`; overwrites shared buffers A/B; blocks on the RPC.

## Important branches
None.

## Constants & flags
- Transport fno `0x37`. `ssize=0x28`, `rsize=0x20`. Inputs at `+0x1c` / `+0x20`; status at `+0x18`.

## Corrected reconstruction
```c
// SN@P transport RPC stub — opcode 0x37: command with two u32 words (caller uses only the first).
u16 snap_rpc_op37_cmd2w(u32 a /*+0x1c*/, u32 b /*+0x20, [inferred] unused at call site*/)
{
    snap_ipc_lock();                                        // WaitSema(0x37aee0)
    *(u32*)(SEND_A + 0x1c) = a;                             // 0x37cf1c
    *(u32*)(SEND_A + 0x20) = b;                             // 0x37cf20
    snap_ipc_call_rpc(0x37, SEND_A, 0x28, RECV_B, 0x20);
    u16 status = *(u16*)(RECV_B + 0x18);                    // 0x37bf18
    snap_ipc_unlock();                                      // SignalSema(0x37aee0)
    return status;
}
// Caller FUN_001ef190(ptr) → op37(*ptr) : first word = *ptr; second word not supplied.
```

## Evidence
- Raw: `uRam0037cf1c = param_1; uRam0037cf20 = param_2; FUN_001ef400(0x37,0x37cf00,0x28,0x37bf00,0x20)`.
- Caller `FUN_001ef190(param_1)` → `FUN_001f0ad0(*param_1)`: only the first arg is passed.
- Confidence **Medium** (mechanism High; the input word's meaning external). Runtime validated: no.

## Remaining uncertainty
Meaning of the first word (an id/handle) and whether the second word is ever used by other (non-observed)
callers is unknown.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001f0ad0.c`  — untouched decompiler output.
