# 0x001a99c0 FUN_001a99c0

| field | value |
|---|---|
| Original address | 0x001a99c0 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001a99c0 |
| Resolved name | handle_table700_add [inferred] |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** — (not captured)
**Callees:** — (none)
**Referenced globals:** handle table at 0x5a9160 (700 × int, -1 = empty)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** 0x5a9160[700]

## Behavioral explanation
Registers an integer handle in a fixed 700-entry table at `0x5a9160`. It linear-scans for the first empty slot (sentinel `-1`) and stores `param_1` there. If the whole table is full (index reaches 700) it returns without inserting — **silent drop, no error signal**. Companion to the 64-entry variant `FUN_001a9a90`/`FUN_001a9ae0`.

## Input / output
- **param_1** (`int`): handle/id to insert.
- **returns:** void (no success/failure indication).

## Side effects
- Writes `param_1` into the first `-1` slot of `0x5a9160`.
- No-op if the table has no free slot.

## Important branches
- `699 < iVar1` → table exhausted → return without inserting.
- `*piVar2 == -1` → free slot found → break and store.

## Constants & flags
- `700` — table capacity.
- `-1` — empty-slot sentinel.
- `0x5a9160` — table base.

## Corrected reconstruction
```c
#define HANDLE_TABLE700  ((int*)0x5a9160)
#define HANDLE_TABLE700_CAP  700
#define SLOT_EMPTY  (-1)

void handle_table700_add(int handle)
{
    int  i = 0;
    int *slot = HANDLE_TABLE700;
    while (i <= HANDLE_TABLE700_CAP - 1) {   // i > 699 => full, return
        if (*slot == SLOT_EMPTY) {           // first free slot
            *slot = handle;
            return;
        }
        i++;
        slot++;
    }
    // table full: silently drop
}
```

## Evidence
- Direct: sentinel `-1`, cap 700, base `0x5a9160`. Structurally identical to the 64-entry `FUN_001a9a90`.
- Runtime-unvalidated (purpose of the handle set not traced).

## Notes / uncertainty
- What the 700 handles represent (fds/sockets/objects) is not established from this function alone.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001a99c0.c`  — untouched decompiler output.
