# 0x0019a1e0 FUN_0019a1e0

| field | value |
|---|---|
| Original address | 0x0019a1e0 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_0019a1e0 |
| Resolved name | tlv_entryB_payload_ptr |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** — (not indexed)
**Callees:** FUN_0019a140 (resolve TLV entry "B")
**Referenced globals:** —
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Twin of `FUN_0019a110` for a different entry: returns a pointer to the payload region (`+0x0c`) of the
TLV entry resolved by `FUN_0019a140()`, or `0` (NULL) if the entry is absent. Same header-skip logic
(`type@+0, count@+4, field8@+8`, payload at `+0xc`).

## Input / output
- **Params:** none.
- **Return:** `int` — `(int)entry + 0x0c` when the entry exists, else `0`.

## Important branches
- `if (entry == 0) return 0; else return entry + 0xc;`

## Constants & flags
- `+0x0c` — payload base offset.

## Corrected reconstruction
```c
void *tlv_entryB_payload_ptr(void)
{
    struct tlv_record *e = tlv_resolve_B();   // FUN_0019a140
    if (e == NULL)
        return NULL;
    return e->payload;                        // (char*)e + 0xc
}
```

## Evidence
- Raw: `FUN_0019a1e0.c` — `lVar2 = FUN_0019a140(); return lVar2 ? lVar2+0xc : 0`.
- Identical to `FUN_0019a110` except the resolver (`FUN_0019a140` vs `FUN_0019a070`).
- Runtime-unvalidated.

## Notes / uncertainty
- Entry "B" tag/namespace (`FUN_0019a140`) not decompiled here; payload contents unknown. Control flow
  trivial/High; identity Medium.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_0019a1e0.c`  — untouched decompiler output.
