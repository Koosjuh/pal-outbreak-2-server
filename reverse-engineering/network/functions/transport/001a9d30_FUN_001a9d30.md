# 0x001a9d30 FUN_001a9d30

| field | value |
|---|---|
| Original address | 0x001a9d30 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001a9d30 |
| Resolved name | slot_alloc_bitmap600 [inferred] |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** — (not captured)
**Callees:** — (none)
**Referenced globals:** busy-byte array at 0x52c1a0 (600 × u8, 0 = free / 1 = in-use)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** 0x52c1a0[600]

## Behavioral explanation
A slot allocator over a 600-entry busy-byte array at `0x52c1a0`. It linear-scans for the first byte equal to `0` (free), marks it `1` (in-use), and returns that index. If no slot is free it returns `0`. The index is masked to 16 bits during increment (`& 0xffff`), harmless given the 600 bound.

## Input / output
- **param:** none.
- **returns:** `uint` — allocated slot index `[0,599]`, or `0` when full.

## Side effects
- Sets `*(u8*)(0x52c1a0 + index) = 1` on success.

## Important branches
- `599 < uVar1` → no free slot → return 0.
- `*(char*)(0x52c1a0 + i) == 0` → free slot → claim and return `i`.

## Constants & flags
- `600` — array capacity.
- byte `0` = free, `1` = in-use.
- `0x52c1a0` — busy-byte array base.

## Corrected reconstruction
```c
#define SLOT_BUSY  ((u8*)0x52c1a0)
#define SLOT_CAP   600

// returns index [0,599], or 0 if full (ambiguous with a valid index 0)
uint slot_alloc_bitmap600(void)
{
    uint i = 0;
    while (i <= SLOT_CAP - 1) {
        if (SLOT_BUSY[i] == 0) {   // first free
            SLOT_BUSY[i] = 1;
            return i;
        }
        i = (i + 1) & 0xffff;
    }
    return 0;                       // full
}
```

## Evidence
- First-fit over `0x52c1a0`, mark `1`, cap 600. Runtime-unvalidated.

## Notes / uncertainty
- Full-table return value `0` collides with a valid index 0; callers presumably guarantee slot 0 stays reserved or check fullness elsewhere.
- The paired free routine (resets a byte to 0) is not this function.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001a9d30.c`  — untouched decompiler output.
