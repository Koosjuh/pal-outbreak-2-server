# 0x001f09f0 snap_rpc_op34_query3w

| field | value |
|---|---|
| Original address | 0x001f09f0 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001f09f0 |
| Resolved name | snap_rpc_op34_query3w |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** FUN_001ef100(dst) → op34(&dst[0], &dst[4], &local)
**Callees:** FUN_001ef3e0 (ipc lock), FUN_001ef400 (snap_ipc_call_rpc), FUN_001ef3f0 (ipc unlock)
**Referenced globals:** 0x37aee0 (IPC sema); 0x37cf00 send A; 0x37bf00 recv B (+0x1c/+0x20/+0x24 words, +0x18 status)
**Referenced strings:** —
**Referenced opcodes:** transport fno 0x34
**State vars:** —

## Behavioral explanation
SN@P transport RPC stub for opcode **0x34** — a query returning three 32-bit words. Sends a header-only
request (`rsize=0x2c` = 0x20 header + three words) and writes `B+0x1c`, `B+0x20`, `B+0x24` back through
`param_1`, `param_2`, `param_3`. The caller keeps the first two words (as adjacent struct fields) and
discards the third into a stack scratch.

## Input / output
- **param_1** (u32*): receives word0 from `B+0x1c` (`uRam0037bf1c`).
- **param_2** (u32*): receives word1 from `B+0x20` (`uRam0037bf20`).
- **param_3** (u32*): receives word2 from `B+0x24` (`uRam0037bf24`).
- **Returns** (u16): IOP status word from `B+0x18`.

## Side effects
- `WaitSema`/`SignalSema` on `0x37aee0`; overwrites shared buffers A/B; blocks; writes the three out-words.

## Important branches
None.

## Constants & flags
- Transport fno `0x34`. `ssize=0x20`, `rsize=0x2c`. Words at `+0x1c`/`+0x20`/`+0x24`; status at `+0x18`.

## Corrected reconstruction
```c
// SN@P transport RPC stub — opcode 0x34: query returning three u32 words.
u16 snap_rpc_op34_query3w(u32 *w0_out, u32 *w1_out, u32 *w2_out)
{
    snap_ipc_lock();                                        // WaitSema(0x37aee0)
    snap_ipc_call_rpc(0x34, SEND_A, 0x20, RECV_B, 0x2c);
    *w0_out = *(u32*)(RECV_B + 0x1c);                       // 0x37bf1c
    *w1_out = *(u32*)(RECV_B + 0x20);                       // 0x37bf20
    *w2_out = *(u32*)(RECV_B + 0x24);                       // 0x37bf24
    u16 status = *(u16*)(RECV_B + 0x18);                    // 0x37bf18
    snap_ipc_unlock();                                      // SignalSema(0x37aee0)
    return status;
}
// Caller FUN_001ef100(dst): dst is a struct { u32 w0; u32 w1; }; w2 goes to a discarded local.
```

## Evidence
- Raw: `FUN_001ef400(0x34,...,0x37bf00,0x2c); *param_1=uRam0037bf1c; *param_2=uRam0037bf20;
  *param_3=uRam0037bf24;` returns `uRam0037bf18`.
- Caller `FUN_001ef100(param_1)` → `op34(param_1, param_1+4, auStack_4)`: two kept words + one discarded.
- Confidence **Medium** (mechanism High; the three words' meaning external). Runtime validated: no.

## Remaining uncertainty
What the three words report (e.g. a status triple or a small record) is unknown; the discarded third word
suggests only two are of interest to the caller.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001f09f0.c`  — untouched decompiler output.
