# 0x001bf180 FUN_001bf180

| field | value |
|---|---|
| Original address | 0x001bf180 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001bf180 |
| Resolved name | FUN_001bf180 (two-stage buffer op) [inferred] |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Low |
| Runtime validated | no |

**Callees:** FUN_001bfe00 (stage 1, no args), FUN_001bfe10 (stage 2, takes param_1).

## Behavioral explanation
Thin two-step dispatcher: runs a preparation/reset step (`FUN_001bfe00`, no arguments) and then a
processing step (`FUN_001bfe10`) that receives the caller's argument. Both callees are outside the
provided decompile set, so their domain roles are unresolved; the control flow (prepare-then-process,
argument forwarded only to the second) is certain.

## Input / output
- **Param 1** (`undefined8`): opaque handle/value forwarded to `FUN_001bfe10`.
- **Return:** void.

## Side effects
Whatever `FUN_001bfe00` / `FUN_001bfe10` do (unresolved). No direct global writes here.

## Important branches
None (straight-line, unconditional).

## Constants & flags
None.

## Corrected reconstruction
```c
// [inferred] prepare, then process param_1
void FUN_001bf180(u64 param_1)
{
    FUN_001bfe00();          // stage 1: reset/prepare (opaque)
    FUN_001bfe10(param_1);   // stage 2: process the argument (opaque)
}
```

## Evidence
- Raw: two unconditional calls. Callees `FUN_001bfe00`/`FUN_001bfe10` not present in the netstack
  decompile export — cannot resolve their behavior. Runtime-unvalidated.

## Notes / uncertainty
Opaque until the two callees are decompiled. Function is a trivial forwarder; kept Low because purpose
is undetermined.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001bf180.c`  — untouched decompiler output.
