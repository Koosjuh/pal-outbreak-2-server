# 0x00186320 FUN_00186320

| field | value |
|---|---|
| Original address | 0x00186320 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_00186320 |
| Resolved name | copy_64byte_block_vu |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** —
**Callees:** — (intrinsics `_lqc2`/`_sqc2` VU load/store quadword)
**Referenced globals:** auRam0027f200 (source 64-byte block: 4 quadwords at 0x27f200/0x27f210/
0x27f220/0x27f230)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** auRam0027f200 (64-byte source constant/state)

## Behavioral explanation
Copies a fixed 64-byte block (four 16-byte quadwords) from the global at `0x27f200` into the caller's
`param_1` buffer, using the EE VU0 macro-mode quadword load/store path (`lqc2`/`sqc2`). It is a
straight 4×16-byte block copy — the VU path is just how the compiler moves 128-bit aligned data on
the EE. The source is a persistent 64-byte region (candidate: a matrix, key schedule, or fixed
parameter table); this routine snapshots it into a destination.

## Input / output
- `param_1` (`undefined1 (*)[16]`) — destination, must hold 4 quadwords (64 bytes), 16-byte aligned.
- Returns `void`.

## Side effects
- Writes 64 bytes at `param_1[0..3]`. Reads (does not modify) `auRam0027f200`.

## Important branches
- None (straight-line, fully unrolled 4× copy).

## Constants & flags
- Source base `0x27f200`, quadwords at `+0x00/+0x10/+0x20/+0x30`.
- Block size = 64 bytes = 4 × 16.

## Corrected reconstruction
```c
extern u128 g_block_27f200[4];          // auRam0027f200 (64 bytes, qword-aligned)

void copy_64byte_block_vu(u128 dst[4])  // param_1
{
    dst[0] = g_block_27f200[0];         // lqc2 0x27f200 ; sqc2 -> dst[0]
    dst[1] = g_block_27f200[1];         // 0x27f210
    dst[2] = g_block_27f200[2];         // 0x27f220
    dst[3] = g_block_27f200[3];         // 0x27f230
}
```

## Evidence
- Lines 10-21: four `_lqc2` loads of `0x27f200..0x27f230` then `_sqc2` stores into `param_1[0..3]`.
  Pure block copy.

## Remaining uncertainty
- Semantic meaning of the 64-byte source (`0x27f200`) — matrix vs key vs parameter set — is not
  determined; the copy mechanics themselves are unambiguous.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_00186320.c`  — untouched decompiler output.
