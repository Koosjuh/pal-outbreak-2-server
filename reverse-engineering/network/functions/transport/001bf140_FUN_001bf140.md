# 0x001bf140 FUN_001bf140

| field | value |
|---|---|
| Original address | 0x001bf140 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001bf140 |
| Resolved name | streambuf_bytes_remaining [inferred] |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** stream-buffer consumers (net RX/TX staging).
**Referenced globals:** — (operates on the passed buffer object).

## Behavioral explanation
Accessor on the EE-side **stream-buffer object** used by the netstack RX/TX staging path. Returns the
number of unread bytes still pending in the buffer: `filled_len - read_cursor`. The buffer-object
field layout is confirmed by the sibling `FUN_001bf230` (compactor) and `FUN_001bf170` (read pointer):
`+0x04` = read cursor, `+0x08` = data base pointer, `+0x0c` = filled length (write end).

## Input / output
- **Param 1** (`int` buf): pointer to a stream-buffer object.
- **Return** (`int`): unread byte count = `*(buf+0x0c) - *(buf+0x04)`.

## Side effects
None (pure read).

## Important branches
None.

## Constants & flags
- `+0x04` read cursor, `+0x0c` filled length — see struct note (cross-checked with `FUN_001bf230`).

## Corrected reconstruction
```c
typedef struct streambuf {   // [inferred] EE netstack RX/TX staging buffer
    /* 0x00 */ int  reserved0;
    /* 0x04 */ int  read_cursor;   // bytes already consumed from the front
    /* 0x08 */ char *data;         // base of the data region
    /* 0x0c */ int  filled_len;    // valid bytes currently in the buffer
} streambuf;

int streambuf_bytes_remaining(streambuf *buf)
{
    return buf->filled_len - buf->read_cursor;   // unread bytes
}
```

## Evidence
- Raw: `return *(int*)(p+0xc) - *(int*)(p+4);`.
- Field semantics corroborated by `FUN_001bf230` (uses +4 as cursor, +8 as base, +0xc as filled) and
  `FUN_001bf170` (base+cursor = read ptr). Runtime-unvalidated.

## Notes / uncertainty
Struct field names are inferred from usage across the sibling functions; offsets are exact.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001bf140.c`  — untouched decompiler output.
