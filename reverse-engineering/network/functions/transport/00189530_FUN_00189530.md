# 0x00189530 FUN_00189530

| field | value |
|---|---|
| Original address | 0x00189530 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_00189530 |
| Resolved name | record_get_count16 |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** — (not indexed)
**Callees:** thunk_FUN_00197ad0 (→ FUN_00197ad0, fetch current tagged-record header)
**Referenced globals:** — (record context is internal to FUN_00197ad0)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Reads a 16-bit count/length field from the *current record header* returned by `thunk_FUN_00197ad0()`.
This header family (fetched by `FUN_00197ad0`) stores its element count as a `short` at offset `+2`
— the same field that `FUN_00189bb0`/`FUN_00189db0` read to size their descriptor-array init loops.
A NULL header yields `0`, so callers can treat "no record / not loaded" as "empty".

## Input / output
- **Params:** none.
- **Return:** `undefined2` (u16) — `header->count16` (`*(short*)(hdr+2)`), or `0` if the header
  pointer is NULL.

## Side effects
- None (pure read). `FUN_00197ad0` may lazily resolve the context but writes nothing here.

## Important branches
- `if (hdr == 0) return 0;` — NULL-guard: no record loaded ⇒ count 0.
- `else return *(short*)(hdr+2);` — normal path.

## Constants & flags
- Offset `+2` — the header's element-count field (u16). Corroborated by `FUN_00189bb0`/`FUN_00189db0`
  reading `sVar1 = *(short*)(rec+2)` as their loop bound.

## Corrected reconstruction
```c
// Header returned by record_ctx_current (thunk_FUN_00197ad0):
struct record_header {
    u16 field0;   // +0x00  [inferred] type/flags
    u16 count16;  // +0x02  element count
    // ... payload follows
};

u16 record_get_count16(void)
{
    struct record_header *hdr = record_ctx_current();  // thunk_FUN_00197ad0
    if (hdr == NULL)
        return 0;
    return hdr->count16;   // *(short*)(hdr + 2)
}
```

## Evidence
- Raw: `FUN_00189530.c` — `lVar2 = thunk_FUN_00197ad0(); if (lVar2==0) 0 else *(u16*)(lVar2+2)`.
- `+2 = count` cross-confirmed by `FUN_00189bb0.c:15-16` / `FUN_00189db0.c:15-16`
  (`sVar1 = *(short*)((int)uVar2 + 2)` used as the descriptor-init loop bound).
- `FUN_00197ad0` categorised `game-engine-uncategorized` (`main-ee-categorized.tsv:3160`).
- Runtime-unvalidated.

## Notes / uncertainty
- Whether the value is a "count" vs. a generic 16-bit field depends on the record type; the
  descriptor-init corroboration makes "element count" the strongest reading. `+0` field unknown.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_00189530.c`  — untouched decompiler output.
