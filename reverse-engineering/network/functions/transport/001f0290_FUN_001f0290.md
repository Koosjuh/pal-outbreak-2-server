# 0x001f0290 FUN_001f0290

| field | value |
|---|---|
| Original address | 0x001f0290 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001f0290 |
| Resolved name | snap_rpc_cmd1f (SN@P RPC selector 0x1f) [inferred] |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callees:** FUN_001ef3e0 (WaitSema), FUN_001ef400 (sceSifCallRpc), FUN_001ef3f0 (SignalSema).
**Referenced globals:** RPC sema `0x37aee0`; req `0x37cf00`; reply `0x37bf00` (status `0x37bf18`).

## Behavioral explanation
SN@P IOP-module RPC stub, **selector `0x1f`**, no arguments. Performs the synchronous SIF RPC with a
`0x20`-byte request and `0x20`-byte reply and returns the module status word. A "no-arg command / poll"
into the SN@P library (e.g. begin/step/acknowledge a transport operation, or query a simple status).
The `0x20` reply length carries only the status word, so no structured data is returned to the caller.

## Input / output
- **Params:** none.
- **Return** (`u16`): SN@P status word (`0x37bf18`).

## Side effects
WaitSema/SignalSema bracket; issues RPC `0x1f`.

## Important branches
None.

## Constants & flags
- `0x1f` = RPC selector. Send/recv len `0x20`/`0x20`. Status @ `0x37bf18`.

## Corrected reconstruction
```c
u16 snap_rpc_cmd1f(void)
{
    snap_rpc_lock();                                       // FUN_001ef3e0
    snap_rpc_call(0x1f, 0x37cf00, 0x20, 0x37bf00, 0x20);  // FUN_001ef400
    u16 status = *(u16*)0x37bf18;
    snap_rpc_unlock();                                     // FUN_001ef3f0
    return status;
}
```

## Evidence
- Raw: `FUN_001ef400(0x1f,0x37cf00,0x20,0x37bf00,0x20)`, returns `uRam0037bf18`. Helpers confirmed.
  Runtime-unvalidated.

## Notes / uncertainty
Domain meaning of selector `0x1f` unresolved; marshalling exact. Sits adjacent to `0x20` (`FUN_001f02e0`)
in the SN@P RPC table — likely a paired transport op (start/stop or query/list).

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001f0290.c`  — untouched decompiler output.
