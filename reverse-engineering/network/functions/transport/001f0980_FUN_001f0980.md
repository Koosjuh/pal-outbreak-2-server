# 0x001f0980 snap_rpc_op33_cmd2w

| field | value |
|---|---|
| Original address | 0x001f0980 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001f0980 |
| Resolved name | snap_rpc_op33_cmd2w |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** FUN_001ef0a0 (thin wrapper; passes no args)
**Callees:** FUN_001ef3e0 (ipc lock), FUN_001ef400 (snap_ipc_call_rpc), FUN_001ef3f0 (ipc unlock)
**Referenced globals:** 0x37aee0 (IPC sema); 0x37cf00 send A (+0x1c u32, +0x20 u32); 0x37bf00 recv B (+0x18 status)
**Referenced strings:** —
**Referenced opcodes:** transport fno 0x33
**State vars:** —

## Behavioral explanation
SN@P transport RPC stub for opcode **0x33** — a command taking two 32-bit input words. It stages
`param_1` at `A+0x1c` and `param_2` at `A+0x20` (`ssize=0x28` = 0x20 header + two words) and returns the
status. The sole caller passes no arguments, so both words are effectively don't-care at the only known
call site.

## Input / output
- **param_1** (u32): staged at `A+0x1c` (`uRam0037cf1c`).
- **param_2** (u32): staged at `A+0x20` (`uRam0037cf20`).
- **Returns** (u16): IOP status word from `B+0x18`.

## Side effects
- `WaitSema`/`SignalSema` on `0x37aee0`; overwrites shared buffers A/B; blocks on the RPC.

## Important branches
None.

## Constants & flags
- Transport fno `0x33`. `ssize=0x28`, `rsize=0x20`. Inputs at `+0x1c` / `+0x20`; status at `+0x18`.

## Corrected reconstruction
```c
// SN@P transport RPC stub — opcode 0x33: command with two u32 input words.
u16 snap_rpc_op33_cmd2w(u32 a /*+0x1c*/, u32 b /*+0x20*/)
{
    snap_ipc_lock();                                        // WaitSema(0x37aee0)
    *(u32*)(SEND_A + 0x1c) = a;                             // 0x37cf1c
    *(u32*)(SEND_A + 0x20) = b;                             // 0x37cf20
    snap_ipc_call_rpc(0x33, SEND_A, 0x28, RECV_B, 0x20);
    u16 status = *(u16*)(RECV_B + 0x18);                    // 0x37bf18
    snap_ipc_unlock();                                      // SignalSema(0x37aee0)
    return status;
}
```

## Evidence
- Raw: `uRam0037cf1c = param_1; uRam0037cf20 = param_2; FUN_001ef400(0x33,0x37cf00,0x28,0x37bf00,0x20)`.
- Caller `FUN_001ef0a0`: pass-through, no args supplied.
- Confidence **Medium** (mechanism High; the two words' meaning external). Runtime validated: no.

## Remaining uncertainty
Meaning of the two input words (an id/value or coordinate pair) is unknown; the argless call site gives no
constraint.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001f0980.c`  — untouched decompiler output.
