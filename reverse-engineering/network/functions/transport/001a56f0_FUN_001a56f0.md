# 0x001a56f0 FUN_001a56f0

| field | value |
|---|---|
| Original address | 0x001a56f0 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001a56f0 |
| Resolved name | rpc_call_sync_3arg (FUN_001a56f0) |
| Subsystem | transport (synchronous SIF-RPC wrapper) |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** — (not captured in this set)
**Callees:** FUN_0019ec60 (acquire/poll flag `0x8000000c`), FUN_001a57c0 (the actual 3-arg operation), FUN_001a2430 (begin drain), FUN_0019eb00 (pending-event query), FUN_00111668 (yield), FUN_001a2490 (end drain)
**Referenced globals:** — (flag id `0xffffffff8000000c` is an immediate)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Identical synchronous wrapper to `FUN_001a4cd0`, but for the **3-arg** operation `FUN_001a57c0`.
It arms the `0x8000000c` completion flag, runs the operation, and — only on success (`result >= 0`) —
opens the drain section and spin-yields until both the pending-event queue (`FUN_0019eb00(1)`) and the
completion flag (`FUN_0019ec60(0x8000000c,1)`) report drained, then closes the section. Returns the
operation's result unchanged.

## Input / output
- `param_1`, `param_2`, `param_3` — forwarded verbatim to `FUN_001a57c0`.
- Returns `long` = `FUN_001a57c0`'s result (`< 0` = failure ⇒ drain skipped; `>= 0` = success).

## Side effects
- Manipulates the `0x8000000c` event/completion flag; enters/leaves the drain section; yields while
  events are pending; plus all effects of `FUN_001a57c0`.

## Important branches
- `FUN_001a57c0(...) < 0` → skip drain, return error.
- `>= 0` → run both pump loops (`FUN_0019eb00(1) > 0`, `FUN_0019ec60(0x8000000c,1) > 0`) to completion.

## Constants & flags
- `0xffffffff8000000c` — sign-extended `0x8000000c` event/completion flag id (bit31 = event-flag marker). [inferred]
- Success threshold `>= 0` (`-1 < lVar1`).

## Corrected reconstruction
```c
// Issue a 3-arg transport op and block until it + its events fully drain. Returns the op result.
long rpc_call_sync_3arg(void *a /*p1*/, void *b /*p2*/, void *c /*p3*/)
{
    FUN_0019ec60(0x8000000c, 1);                 // arm completion flag
    long rc = FUN_001a57c0(a, b, c);             // the actual operation
    if (rc >= 0) {
        FUN_001a2430();                          // begin drain
        while (FUN_0019eb00(1) > 0)     FUN_00111668(0);   // drain events
        while (FUN_0019ec60(0x8000000c,1) > 0) FUN_00111668(0); // drain flag
        FUN_001a2490();                          // end drain
    }
    return rc;
}
```

## Evidence
- Raw lines 8-20: mirror of `FUN_001a4cd0` with `FUN_001a57c0(param_1,param_2,param_3)` as the wrapped
  op; identical `FUN_0019ec60`/`FUN_001a2430`/`FUN_0019eb00`/`FUN_00111668`/`FUN_001a2490` drain.
- Runtime-unvalidated; callee semantics inferred by analogy.

## Remaining uncertainty
- The concrete operation behind `FUN_001a57c0` (3-arg vs the 2-arg `FUN_001a5550`); likely a
  send-with-extra-parameter variant.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001a56f0.c`  — untouched decompiler output.
