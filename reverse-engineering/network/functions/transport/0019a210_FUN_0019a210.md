# 0x0019a210 FUN_0019a210

| field | value |
|---|---|
| Original address | 0x0019a210 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_0019a210 |
| Resolved name | tlv_get_value_tag50000 |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** — (not indexed)
**Callees:** FUN_00199f70 (resolve container node), FUN_0019a350(node, 0x50000, 0) (find-or-load child
by tag)
**Referenced globals:** —
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Returns the scalar value (`+0x04`) of the TLV record for tag `0x50000` under the container node
resolved by `FUN_00199f70()`. NULL-safe at both levels: if the node is absent, or the `0x50000`
record is absent, returns `0`. Unlike `FUN_00199d50`/`FUN_00199df0` (same node + `FUN_0019a350`
idiom) this one does **not** check `node->type == 4` and reads only the count/value word rather than a
payload block — it is the "how many / what value" scalar getter for tag `0x50000`. The list form of
the same tag is `FUN_0019a260`, which copies the `0x50000` payload array out.

## Input / output
- **Params:** none (`FUN_00199f70` called with no meaningful args in this frame).
- **Return:** `undefined4` — `record->value` (`*(u32*)(rec+4)`) for tag `0x50000`; `0` if the node or
  the record is missing.

## Important branches
- `node = FUN_00199f70(); if (node == 0) return 0;`
- `rec = FUN_0019a350(node, 0x50000, 0); if (rec == 0) return 0; else return *(u32*)(rec+4);`

## Constants & flags
- `0x50000` — TLV tag (its payload list is read by `FUN_0019a260`).
- `+0x04` — count/value word.

## Corrected reconstruction
```c
#define TLV_TAG_50000 0x50000

u32 tlv_get_value_tag50000(void)
{
    struct tlv_node *node = tree_resolve();          // FUN_00199f70
    if (node == NULL)
        return 0;
    struct tlv_record *r = node_find_or_load(node, TLV_TAG_50000, 0); // FUN_0019a350
    if (r == NULL)
        return 0;
    return r->value;                                 // *(u32*)(r+4)
}
```

## Evidence
- Raw: `FUN_0019a210.c` — `node = FUN_00199f70(); if(!node) 0; rec = FUN_0019a350(node,0x50000,0);
  if(!rec) 0 else *(u32*)(rec+4)`.
- Tag `0x50000` payload confirmed as a u32 list by `FUN_0019a260.c:16-26` (same node + tag, copies
  `count@+4` words from `payload@+0xc`).
- Runtime-unvalidated.

## Notes / uncertainty
- Meaning of tag `0x50000` (a count/first-value of some list) is inferred from its list twin
  `FUN_0019a260` but not proven. Control flow High; semantics Medium.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_0019a210.c`  — untouched decompiler output.
