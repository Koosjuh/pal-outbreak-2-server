# 0x00184140 FUN_00184140

| field | value |
|---|---|
| Original address | 0x00184140 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_00184140 |
| Resolved name | queue_28a3d0_take |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** —
**Callees:** FUN_0017ffa0 (generic list/queue remove/lookup on object `0x28a3d0`)
**Referenced globals:** 0x28a3d0 (queue/list control object)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** list object 0x28a3d0

## Behavioral explanation
Adapter that removes/looks up `param_1` from the shared list/queue object `0x28a3d0` via
`FUN_0017ffa0(list, item)`. It is the counterpart to `FUN_00184120` (queue_28a3d0_post): the two-arg
primitive (no position selector) matches an unlink-by-node / find-and-remove operation. Return value
of the primitive is discarded here (declared `void`).

## Input / output
- `param_1` — the node/item to remove from (or find in) queue `0x28a3d0`.
- Returns `void`.

## Side effects
- Mutates the `0x28a3d0` list object (unlinks `param_1`).

## Important branches
- None (unconditional tail-call).

## Constants & flags
- `0x28a3d0` — shared list/queue object (same as 0x00184120).

## Corrected reconstruction
```c
extern list_t g_queue_28a3d0;           // 0x28a3d0

void queue_28a3d0_take(void *item)      // param_1
{
    list_remove(&g_queue_28a3d0, item); // FUN_0017ffa0(0x28a3d0, param_1)
}
```

## Evidence
- Line 5: `FUN_0017ffa0(0x28a3d0, param_1)` — two-arg remove/lookup on object `0x28a3d0`.
- Pairs with 0x00184120's insert (`FUN_0017ff80`) on the identical object.

## Remaining uncertainty
- Whether the primitive removes vs merely locates `param_1`, and any return value the wrapper drops,
  is not decoded; "take/remove" inferred from the post/take pairing.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_00184140.c`  — untouched decompiler output.
