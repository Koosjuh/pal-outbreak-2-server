# 0x001f0920 snap_rpc_op2e_query_u32

| field | value |
|---|---|
| Original address | 0x001f0920 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001f0920 |
| Resolved name | snap_rpc_op2e_query_u32 |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** FUN_001ef160 (thin wrapper; passes a scratch out-ptr, discards it)
**Callees:** FUN_001ef3e0 (ipc lock), FUN_001ef400 (snap_ipc_call_rpc), FUN_001ef3f0 (ipc unlock)
**Referenced globals:** 0x37aee0 (IPC sema); 0x37cf00 send A; 0x37bf00 recv B (+0x1c out-word, +0x18 status)
**Referenced strings:** —
**Referenced opcodes:** transport fno 0x2e
**State vars:** —

## Behavioral explanation
SN@P transport RPC stub for opcode **0x2e** — a query that returns one 32-bit result word. Sends a
header-only request, writes the reply word at `B+0x1c` back through `param_1`, and returns the status.
`rsize=0x24` (0x20 header + 4-byte result). The sole caller discards the out-word, using only the status.

## Input / output
- **param_1** (u32*): receives the result word from `B+0x1c` (`uRam0037bf1c`).
- **Returns** (u16): IOP status word from `B+0x18`.

## Side effects
- `WaitSema`/`SignalSema` on `0x37aee0`; overwrites shared buffers A/B; blocks; writes `*param_1`.

## Important branches
None.

## Constants & flags
- Transport fno `0x2e`. `ssize=0x20`, `rsize=0x24`. Result u32 at `+0x1c`; status at `+0x18`.

## Corrected reconstruction
```c
// SN@P transport RPC stub — opcode 0x2e: query returning one u32 result word.
u16 snap_rpc_op2e_query_u32(u32 *result_out)
{
    snap_ipc_lock();                                        // WaitSema(0x37aee0)
    snap_ipc_call_rpc(0x2e, SEND_A, 0x20, RECV_B, 0x24);
    *result_out = *(u32*)(RECV_B + 0x1c);                   // 0x37bf1c
    u16 status = *(u16*)(RECV_B + 0x18);                    // 0x37bf18
    snap_ipc_unlock();                                      // SignalSema(0x37aee0)
    return status;
}
```

## Evidence
- Raw: `FUN_001ef400(0x2e,0x37cf00,0x20,0x37bf00,0x24); *param_1 = uRam0037bf1c;` returns `uRam0037bf18`.
- Caller `FUN_001ef160`: passes a throwaway pointer, keeps only the status.
- Confidence **Medium** (mechanism High; the queried value's meaning external). Runtime validated: no.

## Remaining uncertainty
What the returned u32 reports (a count, handle, or status detail) is unknown; the caller ignoring it
suggests it may be redundant/optional.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001f0920.c`  — untouched decompiler output.
