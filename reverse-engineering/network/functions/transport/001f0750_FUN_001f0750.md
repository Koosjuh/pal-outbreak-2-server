# 0x001f0750 snap_rpc_op53_cmd

| field | value |
|---|---|
| Original address | 0x001f0750 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001f0750 |
| Resolved name | snap_rpc_op53_cmd |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** FUN_001eee60 (thin wrapper, no flag write)
**Callees:** FUN_001ef3e0 (ipc lock), FUN_001ef400 (snap_ipc_call_rpc), FUN_001ef3f0 (ipc unlock)
**Referenced globals:** 0x37aee0 (IPC sema); 0x37cf00 send A; 0x37bf00 recv B (+0x18 status)
**Referenced strings:** —
**Referenced opcodes:** transport fno 0x53
**State vars:** —

## Behavioral explanation
SN@P transport RPC stub for opcode **0x53** — a parameterless command/query. Sends a header-only request
and returns the IOP status word. Its wrapper `FUN_001eee60` adds nothing (no flag/state write), so this
is the most opaque member of the batch: fno 0x53 sits well above the room-op cluster (0x25–0x3a) and its
IOP-side meaning is not recoverable from this binary.

## Input / output
- **Inputs:** none.
- **Returns** (u16): IOP status word from `B+0x18`.

## Side effects
- `WaitSema`/`SignalSema` on `0x37aee0`; overwrites shared buffers A/B; blocks on the RPC.

## Important branches
None.

## Constants & flags
- Transport fno `0x53`. `ssize=0x20`, `rsize=0x20`. Status at `+0x18`.

## Corrected reconstruction
```c
// SN@P transport RPC stub — opcode 0x53: parameterless command/query. IOP-side meaning unknown.
u16 snap_rpc_op53_cmd(void)
{
    snap_ipc_lock();                                       // WaitSema(0x37aee0)
    snap_ipc_call_rpc(0x53, SEND_A, 0x20, RECV_B, 0x20);   // header-only round trip
    u16 status = *(u16*)(RECV_B + 0x18);                   // 0x37bf18
    snap_ipc_unlock();                                     // SignalSema(0x37aee0)
    return status;
}
```

## Evidence
- Raw: `FUN_001ef400(0x53,0x37cf00,0x20,0x37bf00,0x20)`, returns `uRam0037bf18`.
- Caller `FUN_001eee60`: pure pass-through, no side effect.
- Confidence **Medium** (RPC mechanism High; opcode meaning unknown). Runtime validated: no.

## Remaining uncertainty
No caller context or flag write to anchor a meaning. fno 0x53 is outside the contiguous 0x25–0x3a room-op
range, hinting at a different IOP handler group (status/reset/version query are candidates, all unproven).

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001f0750.c`  — untouched decompiler output.
