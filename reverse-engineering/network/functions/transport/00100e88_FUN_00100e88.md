# 0x00100e88 net_rpc_bind_svc593

| field | value |
|---|---|
| Original address | 0x00100e88 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_00100e88 |
| Resolved name | net_rpc_bind_svc593 |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** — (indirect / table)
**Callees:** FUN_00100590 (net_rpc_lock), FUN_00100e18 (rpc_thread_ensure2), FUN_00115300 (sif_dma_sync), FUN_00115a98 (sif_rpc_bind), FUN_00114ac8 (net_tracef)
**Referenced globals:** DAT_002074ac (semaphore B), DAT_00207498 (cmd word B), DAT_002074cc (svc 0x593 bound-flag), client node 0x209000, ready word 0x209024
**Referenced strings:** trace fmts 0x24e9f8, 0x24ea20
**Referenced opcodes:** —
**State vars:** ram0x00207490 (debug gate)

## Behavioral explanation
Structural twin of `net_rpc_bind_svc595` (0x00100b70) but for **service 0x80000593** on a **second RPC mutex** (`DAT_002074ac`, "channel B") and its own command word `DAT_00207498`, bound-flag `DAT_002074cc`, client node `0x209000`, ready word `0x209024`. Bind/attach-only readiness gate; returns 1 on ready. The 593/595 pair on separate semaphores indicates two independent IOP RPC channels (request vs. notify, or two device instances).

## Input / output
- `param_1` : `u32` command/context word → `DAT_00207498`.
- **return** : `1` when bound & ready; `0` on mutex-not-owned or thread-ensure failure.

## Side effects / branches
- WaitSema/SignalSema on `DAT_002074ac`.
- `PollSema != handle` → debug-trace `0x24e9f8`, return 0.
- `rpc_thread_ensure2(1) != 0` → SignalSema, return 0.
- `DAT_002074cc >= 0` → fast return 1 (mutex retained, same idiom as sibling).
- else bind-spin `sif_rpc_bind(0x209000, 0x80000593)` until `DAT_00209024 != 0`; latch `DAT_002074cc = 0`; return 1.

## Constants & flags
- `0x80000593` — SIF-RPC service id, channel B.

## Corrected reconstruction
```c
// Lazy bind / readiness gate for IOP SIF-RPC service 0x80000593 (RPC channel B).
u32 net_rpc_bind_svc593(u32 ctx)
{
    net_rpc_lock();
    if (PollSema(DAT_002074ac) != DAT_002074ac) {
        if (dbg) net_tracef(FMT_notowner_b, ctx, g_net_cmd_b);
        return 0;
    }
    g_net_cmd_b = ctx;                                // DAT_00207498
    ReferThreadStatus(g_rpc_thread, &g_rpc_thread_status);
    if (rpc_thread_ensure2(1) != 0) { SignalSema(DAT_002074ac); return 0; }
    sif_dma_sync(0);

    if (svc593_bound_flag >= 0)                        // DAT_002074cc
        return 1;

    while (1) {
        while (sif_rpc_bind(&client_593, 0x80000593, 0) < 0) {
            if (dbg) net_tracef(FMT_bind_retry_b);
            delay_spin(0x100000);
        }
        if (client_593.server_ready != 0) break;      // DAT_00209024
        delay_spin(0x100000);
    }
    svc593_bound_flag = 0;
    return 1;
}
```

## Evidence
- Raw decompile FUN_00100e88.c; byte-for-byte sibling of 0x00100b70 on distinct globals/service.
- Confidence **Medium**: mechanics firm; channel-B role unresolved.

## Remaining uncertainty
Division of labor between service 0x593 (channel B) and 0x595 (channel A); identity of both.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_00100e88.c`  — untouched decompiler output.
