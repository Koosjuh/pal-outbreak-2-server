# 0x001a8730 FUN_001a8730

| field | value |
|---|---|
| Original address | 0x001a8730 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001a8730 |
| Resolved name | is_idle (FUN_001a8730) |
| Subsystem | transport (status predicate) |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** — (not captured in this set)
**Callees:** FUN_001a8760 (status query)
**Referenced globals:** —
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
A one-line predicate: returns `true` iff the status query `FUN_001a8760()` returns `0`. In this
codebase's convention a `0` from a status/handle query means "idle / nothing pending / complete", so
this reads as "is the subsystem idle?" — the boolean complement of a busy check. Typically polled
before starting a new operation or to detect completion.

## Input / output
- No parameters.
- Returns `bool`: `true` iff `FUN_001a8760() == 0`, else `false`.

## Side effects
- None (pure query wrapper).

## Important branches
- Single comparison `FUN_001a8760() == 0`; no control flow.

## Constants & flags
- `0` — the "idle/none/complete" sentinel returned by `FUN_001a8760`. [inferred]

## Corrected reconstruction
```c
// True when the underlying subsystem reports idle (status query == 0).
bool is_idle(void)
{
    return FUN_001a8760() == 0;
}
```

## Evidence
- Raw lines 6-8: `lVar1 = FUN_001a8760(); return lVar1 == 0;`.
- `FUN_001a8760` is outside this decompile set; the `== 0`-means-idle reading is a convention-based
  inference. Runtime-unvalidated.

## Remaining uncertainty
- Exactly what `FUN_001a8760` reports (queue depth? active-handle count? busy flag) — the `0` sentinel
  is assumed to mean idle/complete.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001a8730.c`  — untouched decompiler output.
