# 0x001f0450 FUN_001f0450

| field | value |
|---|---|
| Original address | 0x001f0450 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001f0450 |
| Resolved name | snap_rpc_cmd22 (SN@P RPC selector 0x22) [inferred] |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callees:** FUN_001ef3e0 (WaitSema), FUN_001ef400 (sceSifCallRpc), FUN_001ef3f0 (SignalSema).
**Referenced globals:** RPC sema `0x37aee0`; req `0x37cf00`; reply `0x37bf00` (status `0x37bf18`).

## Behavioral explanation
SN@P IOP-module RPC stub, **selector `0x22`**, no arguments. Synchronous SIF RPC, `0x20`-byte request
and reply, returns the module status word only. A no-arg command into the SN@P library — given it
immediately follows the compound room-submit (`0x21`) in the RPC table, a plausible role is
"commit / confirm / cancel" of a pending transport operation, but this is not runtime-confirmed. Only
the status is returned.

## Input / output
- **Params:** none.
- **Return** (`u16`): SN@P status word (`0x37bf18`).

## Side effects
WaitSema/SignalSema bracket; issues RPC `0x22`.

## Important branches
None.

## Constants & flags
- `0x22` = RPC selector. Send/recv `0x20`/`0x20`. Status @ `0x37bf18`.

## Corrected reconstruction
```c
u16 snap_rpc_cmd22(void)
{
    snap_rpc_lock();                                       // FUN_001ef3e0
    snap_rpc_call(0x22, 0x37cf00, 0x20, 0x37bf00, 0x20);  // FUN_001ef400
    u16 status = *(u16*)0x37bf18;
    snap_rpc_unlock();                                     // FUN_001ef3f0
    return status;
}
```

## Evidence
- Raw: `FUN_001ef400(0x22,0x37cf00,0x20,0x37bf00,0x20)`, returns `uRam0037bf18`. Helpers confirmed.
  Runtime-unvalidated.

## Notes / uncertainty
Domain meaning of selector `0x22` unresolved. Marshalling exact.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001f0450.c`  — untouched decompiler output.
