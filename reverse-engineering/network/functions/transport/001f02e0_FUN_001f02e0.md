# 0x001f02e0 FUN_001f02e0

| field | value |
|---|---|
| Original address | 0x001f02e0 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001f02e0 |
| Resolved name | snap_rpc_cmd20 (SN@P RPC selector 0x20) [inferred] |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callees:** FUN_001ef3e0 (WaitSema), FUN_001ef400 (sceSifCallRpc), FUN_001ef3f0 (SignalSema).
**Referenced globals:** RPC sema `0x37aee0`; req `0x37cf00`; reply `0x37bf00` (status `0x37bf18`).

## Behavioral explanation
SN@P IOP-module RPC stub, **selector `0x20`**, no arguments. Synchronous SIF RPC, `0x20`-byte request
and reply, returns the module status word. Task briefing associates transport op `0x20` with the
room-**list** family (`0x1c/0x1e/0x20 list`), so this is the likely EE trigger for a list/enumerate
transport operation whose actual entries are then drained via the event pump (`FUN_001f0070`,
selector `0x1b`). Only the status is returned here.

## Input / output
- **Params:** none.
- **Return** (`u16`): SN@P status word.

## Side effects
WaitSema/SignalSema bracket; issues RPC `0x20`.

## Important branches
None.

## Constants & flags
- `0x20` = RPC selector (room-list family per briefing). Send/recv `0x20`/`0x20`. Status @ `0x37bf18`.

## Corrected reconstruction
```c
u16 snap_rpc_cmd20(void)   // [inferred] transport list/enumerate trigger
{
    snap_rpc_lock();                                       // FUN_001ef3e0
    snap_rpc_call(0x20, 0x37cf00, 0x20, 0x37bf00, 0x20);  // FUN_001ef400
    u16 status = *(u16*)0x37bf18;
    snap_rpc_unlock();                                     // FUN_001ef3f0
    return status;
}
```

## Evidence
- Raw: `FUN_001ef400(0x20,0x37cf00,0x20,0x37bf00,0x20)`, returns `uRam0037bf18`. Helpers confirmed.
- Association with the list op-group is from the task briefing (`0x1c/0x1e/0x20 list`), not runtime.

## Notes / uncertainty
Whether selector `0x20` == the wire "list" op or is an internal command id is unverified; results are
retrieved separately via the event pump. Marshalling exact.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001f02e0.c`  — untouched decompiler output.
