# 0x001a0e90 FUN_001a0e90

| field | value |
|---|---|
| Original address | 0x001a0e90 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001a0e90 |
| Resolved name | asset_request_chanB (FUN_001a0e90) |
| Subsystem | transport (asset/RPC request channel) |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** FUN_001a1750 (non-0x10000 path — uses return as reply length `> 0`)
**Callees:** FUN_001a08d0 (core request/transfer on channel handle `uRam0029bc18`)
**Referenced globals:** `uRam0029bc18` (channel-B handle)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Sibling of `FUN_001a0df0` for **channel B** (`uRam0029bc18`). It issues the request/transfer directly
via `FUN_001a08d0(uRam0029bc18, request, dest, 0)` with **no** `FUN_001a0bf0` pre-step. Used by
`FUN_001a1750` when a named-resource lookup takes the non-`0x10000` format path. Returns the reply
byte length from `FUN_001a08d0` (the `void` type is a decompiler artifact; the caller branches on the
result being `> 0`).

## Input / output
- `param_1` (ptr) — request descriptor/string.
- `param_2` (ptr) — destination/work buffer for the reply (`iRam003435bc` at the call site).
- Returns (long, lost as `void`) — reply length from `FUN_001a08d0`; `> 0` = success.

## Side effects
- `FUN_001a08d0` transfers on channel-B handle `uRam0029bc18` and writes into `param_2`.

## Important branches
- None (straight-line delegate).

## Constants & flags
- `uRam0029bc18` — channel-B handle (contrast `uRam0029bc20` = channel A).
- trailing `0` arg to `FUN_001a08d0` — flags/mode (0 = default). [inferred]

## Corrected reconstruction
```c
// Channel-B asset/RPC request (no arming pre-step). Returns reply byte length.
long asset_request_chanB(void *request /*param_1*/, void *dest /*param_2*/)
{
    return FUN_001a08d0(uRam0029bc18, request, dest, 0);
}
```

## Evidence
- Raw lines 5-6: `FUN_001a08d0(uRam0029bc18,param_1,param_2,0);`.
- Return-value fact: caller `FUN_001a1750` (`lVar4 = FUN_001a0e90(...); if (0 < lVar4)`).
- Channel A/B pairing with `FUN_001a0df0` via `uRam0029bc20`/`uRam0029bc18`. Runtime-unvalidated.

## Remaining uncertainty
- Whether channel B differs from A only by handle, or also by protocol/format (the caller selects it
  specifically for the non-`0x10000` path, hinting at a format distinction).

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001a0e90.c`  — untouched decompiler output.
