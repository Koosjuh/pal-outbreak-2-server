# 0x001b2470 FUN_001b2470

| field | value |
|---|---|
| Original address | 0x001b2470 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001b2470 |
| Resolved name | pump_frames [inferred] |
| Subsystem | transport (mis-tagged — frame/tick pump) |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** — (not captured)
**Callees:** FUN_00195f90 (advance one frame/tick, arg 0), FUN_001a32d0 (sample a status/time value)
**Referenced globals:** cRam00343560 (per-iteration clear gate), uRam0027e580 (cleared when gate set), uRam00343594 (refreshed with FUN_001a32d0 result)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** 0x27e580, 0x343594

## Behavioral explanation
Runs the frame/tick pump `param_1` times. On each iteration, if the gate `cRam00343560` is non-zero it first clears `uRam0027e580` to 0, then calls `FUN_00195f90(0)` to advance one frame/tick. After the loop it refreshes `uRam00343594` from `FUN_001a32d0()` (a status/time snapshot). Effectively "step the system forward N frames", used to wait/idle a fixed number of ticks while keeping per-frame state maintained.

## Input / output
- **param_1** (`int`): number of frames/ticks to advance.
- **returns:** void.

## Side effects
- Calls `FUN_00195f90(0)` `param_1` times.
- Conditionally clears `uRam0027e580` each iteration (when `cRam00343560 != 0`).
- Sets `uRam00343594 = FUN_001a32d0()` after looping.

## Important branches
- `iVar1 < param_1` — loop bound (no-op when `param_1 <= 0`).
- `cRam00343560 != '\0'` → clear `uRam0027e580` before the tick.

## Constants & flags
- gate `cRam00343560` — enables per-iteration `0x27e580` clear.

## Corrected reconstruction
```c
void pump_frames(int nframes)
{
    for (int i = 0; i < nframes; i++) {
        if (cRam00343560 != 0)
            uRam0027e580 = 0;      // gated per-frame clear
        FUN_00195f90(0);           // advance one frame/tick
    }
    uRam00343594 = FUN_001a32d0(); // refresh status/time snapshot
}
```

## Evidence
- Fixed-count loop around a single-tick advance (`FUN_00195f90(0)`) with a post-loop global refresh. Runtime-unvalidated.

## Notes / uncertainty
- Precise roles of `FUN_00195f90` (render/vsync vs logic tick), `FUN_001a32d0` (time vs status), and flags `0x343560`/`0x27e580`/`0x343594` are not decoded; `transport` tag looks incorrect.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001b2470.c`  — untouched decompiler output.
