# 0x001bf170 FUN_001bf170

| field | value |
|---|---|
| Original address | 0x001bf170 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001bf170 |
| Resolved name | streambuf_read_ptr [inferred] |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** stream-buffer consumers (net RX/TX staging).
**Referenced globals:** — (operates on the passed buffer object).

## Behavioral explanation
Accessor on the EE-side **stream-buffer object** (same struct as `FUN_001bf140`/`FUN_001bf230`).
Returns the absolute pointer to the current read position: `data_base + read_cursor`. Callers use it
to read/parse the next unconsumed bytes before advancing the cursor via `FUN_001bf230`.

## Input / output
- **Param 1** (`int` buf): pointer to a stream-buffer object.
- **Return** (`int`): pointer to current read position = `*(buf+0x08) + *(buf+0x04)`.

## Side effects
None (pure read).

## Important branches
None.

## Constants & flags
- `+0x04` read cursor, `+0x08` data base — see struct note in `FUN_001bf140`.

## Corrected reconstruction
```c
// returns absolute address of the next unread byte in the stream buffer
char *streambuf_read_ptr(streambuf *buf)   // struct as in FUN_001bf140
{
    return buf->data + buf->read_cursor;
}
```

## Evidence
- Raw: `return *(int*)(p+8) + *(int*)(p+4);`.
- Field roles cross-checked with `FUN_001bf230` (base +8, cursor +4). Runtime-unvalidated.

## Notes / uncertainty
Return typed as pointer by intent; decompiler renders `int`. Offsets exact.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001bf170.c`  — untouched decompiler output.
