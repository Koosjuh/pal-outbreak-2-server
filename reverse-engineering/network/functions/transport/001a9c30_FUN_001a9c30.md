# 0x001a9c30 FUN_001a9c30

| field | value |
|---|---|
| Original address | 0x001a9c30 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001a9c30 |
| Resolved name | fifo_pop_u32 [inferred] |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** — (not captured)
**Callees:** — (none)
**Referenced globals:** sRam0052d610 (queue length, short), puRam0052c400 (queue head pointer, u32*)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** queue buffer at 0x52c400, count 0x52d610, head cursor 0x52c400 (self-advancing)

## Behavioral explanation
Pops one 32-bit word from the front of a linear FIFO. If the length `sRam0052d610` is zero the queue is empty and it returns 0. Otherwise it reads the head word `*puRam0052c400`, **zeroes that slot**, advances the head pointer by one word, decrements the length, and returns the popped value. The head pointer is itself a mutable global, so the queue drains forward through the backing buffer (no wrap here — this is a consume-once linear queue, not a ring).

## Input / output
- **param:** none.
- **returns:** `undefined4` — the popped word, or `0` when empty.

## Side effects
- `*puRam0052c400 = 0` (clears the consumed slot).
- `puRam0052c400 += 1` (advance head).
- `sRam0052d610 -= 1` (shrink length).

## Important branches
- `sRam0052d610 == 0` → empty → return 0 (no state change). Note: return value `0` is ambiguous with a legitimately-queued `0`.

## Constants & flags
- Empty encoded purely by `sRam0052d610 == 0`.

## Corrected reconstruction
```c
extern short   fifo_len;     // sRam0052d610
extern u32    *fifo_head;    // puRam0052c400 (advances as it drains)

u32 fifo_pop_u32(void)
{
    if (fifo_len == 0)
        return 0;            // empty
    u32 v = *fifo_head;
    *fifo_head = 0;          // clear consumed slot
    fifo_head++;             // advance head
    fifo_len--;
    return v;
}
```

## Evidence
- Length-gated dequeue with head-advance and slot-clear. `0x52c400` backing buffer, `0x52d610` count.
- Runtime-unvalidated.

## Notes / uncertainty
- The producer side (who fills `0x52c400` / resets the head) is not in this function; whether the buffer is reset per cycle is unknown.
- Empty vs. popped-zero is indistinguishable by return value alone.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001a9c30.c`  — untouched decompiler output.
