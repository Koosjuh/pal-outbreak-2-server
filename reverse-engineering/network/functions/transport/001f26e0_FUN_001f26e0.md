# 0x001f26e0 FUN_001f26e0

| field | value |
|---|---|
| Original address | 0x001f26e0 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001f26e0 |
| Resolved name | iopdev_set_params |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** — (subsystem API)
**Callees:** — (none; pure EE-local writes)
**Referenced globals:** 0x37e944 (device-table base)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** device table (per-index +0x14/+0x18/+0x1c)

## Behavioral explanation
**Local-only** setter (no RPC): writes two per-device parameters into the control block for `index`
and clears an associated status word. Sets `table[index].param_a (+0x18) = param_2`,
`table[index].param_b (+0x1c) = param_3`, and `table[index].w14 (+0x14) = 0`. The two params are
validated non-negative; there is **no index bounds check** (unlike the sibling setters), so the caller
must pass a valid index. These fields are staged in EE RAM and presumably consumed by a later RPC
(e.g. open/set-mode) or the VBLANK handler.

## Input / output
- `param_1` — device index (unchecked).
- `param_2` — parameter A, must be `>= 0`; stored at +0x18.
- `param_3` — parameter B, must be `>= 0`; stored at +0x1c.
- **Returns** `u32`: `0` on success, `-0xb` (0xfffffff5) if `param_2 < 0`, `-0xc` (0xfffffff4) if
  `param_3 < 0`.

## Side effects
- `table[index].w14 = 0` (+0x14).
- `table[index].param_a = param_2` (+0x18).
- `table[index].param_b = param_3` (+0x1c).

## Important branches
- `param_2 < 0` -> return -0xb (no writes).
- `param_3 < 0` -> return -0xc (no writes).
- else -> perform the three writes, return 0.

## Constants & flags
- Field offsets +0x14 (cleared), +0x18 (param A), +0x1c (param B) within the 0x38-byte block.
- `-0xb`/`-0xc` = the subsystem's OOB/invalid-value error codes.

## Corrected reconstruction
```c
// Stage two per-device parameters locally (no RPC). NOTE: no index bounds check.
uint32_t iopdev_set_params(int index, long param_a, long param_b) {  // [inferred name]
    if (param_a < 0) return 0xfffffff5;   // -0xb
    if (param_b < 0) return 0xfffffff4;   // -0xc
    iopdev_block *b = &g_iopdev_table[index];   // index*0x38 + 0x37e944  (UNCHECKED)
    b->w14     = 0;         // +0x14
    b->param_a = param_a;   // +0x18
    b->param_b = param_b;   // +0x1c
    return 0;
}
```

## Evidence
- Raw: `if (param_2 < 0) return 0xfffffff5;` / `if (param_3 < 0) return 0xfffffff4;`
  / `iVar1 = param_1*0x38 + iRam0037e944;` / `*(iVar1+0x1c)=param_3; *(iVar1+0x18)=param_2; *(iVar1+0x14)=0;`
  / return 0.
- Table base 0x37e944 + stride 0x38 consistent with iopdev_rpc_init / set_mode.
- Runtime-unvalidated.

## Remaining uncertainty
- **Missing index bounds check** is a real divergence from siblings (0x1f2728/0x1f2778 both gate on
  count) — either intentional (trusted caller) or a latent OOB write. Flagged, not asserted.
- Meaning of param_a/param_b (+0x18/+0x1c) undetermined. Medium.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001f26e0.c`  — untouched decompiler output.
