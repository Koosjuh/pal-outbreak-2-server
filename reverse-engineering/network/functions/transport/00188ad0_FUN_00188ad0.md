# 0x00188ad0 FUN_00188ad0

| field | value |
|---|---|
| Original address | 0x00188ad0 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_00188ad0 |
| Resolved name | record_subsystem_reset_wrapper |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** — (not indexed)
**Callees:** thunk_FUN_00197ae0 (→ FUN_00197ae0, tagged-record subsystem accessor)
**Referenced globals:** — (none directly; state lives behind thunk_FUN_00197ae0)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Trivial forwarding wrapper. It calls the tagged-record subsystem entry point `thunk_FUN_00197ae0()`
(discarding its return) and unconditionally returns `1` (success). This is the classic shape of an
"ensure/reset/select the record subsystem" shim: the real work happens as a side effect inside
`FUN_00197ae0` (selecting or (re)initialising the current tagged-record context that the sibling
accessors — `FUN_00189530`, `FUN_00199xxx`, `FUN_0019axxx` — then read from), and the caller only
needs a boolean "OK".

## Input / output
- **Params:** none (`void`).
- **Return:** `undefined4` — constant `1`. Interpreted as a success/`TRUE` status.

## Side effects
- Whatever `FUN_00197ae0` does (record-context select/init). No globals written in this frame.

## Important branches
- None. Straight-line: call, return 1. No error path exists — this function can never report failure.

## Constants & flags
- `1` — success sentinel returned to callers that gate on a boolean.

## Corrected reconstruction
```c
// Ensure/select the tagged-record subsystem context, then report success.
// The meaningful effect is inside record_ctx_select(); this shim only returns TRUE. [inferred]
u32 record_subsystem_reset_wrapper(void)
{
    record_ctx_select();   // thunk_FUN_00197ae0 — side effect only, return discarded
    return 1;              // always success
}
```

## Evidence
- Raw: `FUN_00188ad0.c` — body is exactly `thunk_FUN_00197ae0(); return 1;`.
- `thunk_FUN_00197ae0` → `FUN_00197ae0` is categorised `game-engine-uncategorized`
  (`iso/inventory/main-ee-categorized.tsv:3161`); it is the sibling of `FUN_00197ad0` used by
  `FUN_00189530`/`FUN_00189bb0` to fetch the header record whose count sits at `+2`.
- Runtime-unvalidated.

## Notes / uncertainty
- The exact semantics of `FUN_00197ae0` (select vs. reset vs. lazy-init the record context) are not
  decompiled here; the return-`1`-always shape is certain, the *purpose* is inferred. Low semantic
  confidence, high control-flow confidence.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_00188ad0.c`  — untouched decompiler output.
