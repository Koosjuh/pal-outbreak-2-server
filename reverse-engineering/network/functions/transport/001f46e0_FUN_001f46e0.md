# 0x001f46e0 FUN_001f46e0

| field | value |
|---|---|
| Original address | 0x001f46e0 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001f46e0 |
| Resolved name | netmod_state_reset |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** — (not resolved in this batch)
**Callees:** — (leaf)
**Referenced globals:** uRam00390800, uRam00390804, uRam003908cc, uRam00390824, uRam00390814
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
A small state-reset / init for a netstack module built around the `0x390800` global block. It clears
two counters/heads (`0x390800`, `0x390804`), raises a "enabled/ready" flag (`0x3908cc = 1`), invalidates
a handle/index to the sentinel `0xffffffff` (`0x390824`), and clears another field (`0x390814`). No
inputs, no return — a straight-line initialiser called when the module is (re)started or torn down to a
known-idle baseline.

## Input / output
- No parameters, `void` return.

## Side effects
- `uRam00390800 = 0`, `uRam00390804 = 0`, `uRam003908cc = 1`, `uRam00390824 = 0xffffffff`,
  `uRam00390814 = 0`.

## Important branches
- None (straight-line).

## Constants & flags
- `0xffffffff` — sentinel "no handle / invalid index" written to `0x390824`.
- `1` — enabled/ready flag written to `0x3908cc`.

## Corrected reconstruction
```c
void netmod_state_reset(void)
{
    g_mod.head      = 0;            // uRam00390800
    g_mod.tail      = 0;            // uRam00390804
    g_mod.enabled   = 1;            // uRam003908cc
    g_mod.cur_handle = 0xffffffff;  // uRam00390824 (invalid)
    g_mod.field814  = 0;            // uRam00390814
}
```

## Evidence
- Raw lines 5-9: the five global stores exactly as reconstructed (two zeros, `0x3908cc=1`,
  `0x390824=0xffffffff`, `0x390814=0`).

## Remaining uncertainty
- The role of the `0x390800` block and each field (queue head/tail vs config) is inferred from the
  reset pattern (zero counters, set enabled, invalidate handle); no caller or reader resolved in this
  batch to confirm the semantics.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001f46e0.c`  — untouched decompiler output.
