# 0x0019a110 FUN_0019a110

| field | value |
|---|---|
| Original address | 0x0019a110 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_0019a110 |
| Resolved name | tlv_entryA_payload_ptr |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** — (not indexed)
**Callees:** FUN_0019a070 (resolve TLV entry "A")
**Referenced globals:** —
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Returns a **pointer to the payload region** (`+0x0c`) of the TLV entry resolved by `FUN_0019a070()`,
or `0` (NULL) if that entry does not exist. `FUN_0019a070` is a specific-tag resolver in the accessor
family (the "A" entry); this shim converts the resolved record into a caller-usable data pointer that
skips the 12-byte header (`type@+0, count@+4, field8@+8`). Callers then read the payload array
directly (contrast `FUN_001998c0`, which does the bounds-checked indexing internally).

## Input / output
- **Params:** none.
- **Return:** `int` — `(int)entry + 0x0c` (pointer to payload) when the entry exists, else `0`.

## Important branches
- `if (entry == 0) return 0; else return entry + 0xc;` — NULL-safe payload pointer.

## Constants & flags
- `+0x0c` — payload base offset (past the `type/count/field8` header).

## Corrected reconstruction
```c
void *tlv_entryA_payload_ptr(void)
{
    struct tlv_record *e = tlv_resolve_A();   // FUN_0019a070
    if (e == NULL)
        return NULL;
    return e->payload;                        // (char*)e + 0xc
}
```

## Evidence
- Raw: `FUN_0019a110.c` — `lVar2 = FUN_0019a070(); return lVar2 ? lVar2+0xc : 0`.
- `+0xc` payload base shared across the family (`FUN_001998c0`, `FUN_0019a260`, `FUN_00199df0`).
- Runtime-unvalidated.

## Notes / uncertainty
- Which entry `FUN_0019a070` resolves (its tag / namespace) is not decompiled here; hence the payload
  contents are unknown. Twin of `FUN_0019a1e0` (entry "B"). Control flow trivial/High; identity Medium.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_0019a110.c`  — untouched decompiler output.
