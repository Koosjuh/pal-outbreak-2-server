# 0x001ae3e0 FUN_001ae3e0

| field | value |
|---|---|
| Original address | 0x001ae3e0 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001ae3e0 |
| Resolved name | measure_text_width [inferred] |
| Subsystem | transport (mis-tagged — text metrics) |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** — (not captured)
**Callees:** FUN_001ade00 (decode next glyph from cursor, returns glyph or 0 at end), FUN_001adf30 (map glyph → font-table key), FUN_001ae1a0 (look up glyph metrics; 0 ⇒ full-width), FUN_0010a050 (fallback plain width, e.g. strlen-based)
**Referenced globals:** iRam002a6598 (text/encoding mode; == 2 selects the rich path), iRam002a6628 (width-mode flag), font context 0x2a6590
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** font/encoding context 0x2a6590

## Behavioral explanation
Computes the display **width** (in half-width cell units) of a string. When the encoding mode `iRam002a6598 == 2` it walks the string glyph-by-glyph via `FUN_001ade00`, accumulating a per-glyph advance:
- If `iRam002a6628 == 0` (fast ASCII/range path): glyphs `< 0x10` count as 2, glyphs in `[0x10,0x7f]` count as 1, and glyphs `>= 0x80` fall through to the table path.
- Otherwise (flag set, or a wide glyph `>= 0x80`): map the glyph through the font table (`FUN_001adf30` → `FUN_001ae1a0`); a lookup result of 0 counts as 2 (full-width), non-zero counts as 1 (half-width).

When the mode is not 2 it returns `FUN_0010a050()` — a simple/legacy width (plain character count). This is a classic half/full-width text metric used to align or clip UI strings under a mixed ASCII/multi-byte (SJIS-like) encoding.

## Input / output
- **param_1**: string / cursor start passed to the glyph iterator (via stack `uStack_4`).
- **returns:** `int` — accumulated display width in half-width units.

## Side effects
- None persistent (reads font context `0x2a6590`; iterator advances a local cursor copy).

## Important branches
- `iRam002a6598 == 2` → rich per-glyph measuring; else → `FUN_0010a050()` fallback.
- Inner: `iRam002a6628 == 0` → range-based fast path (`<0x10`→+2, `<0x80`→+1, `>=0x80`→table); else → table path directly.
- Table path: `FUN_001ae1a0(...) == 0` → +2 (full-width) else +1 (half-width).
- Loop ends when `FUN_001ade00` returns 0 (end of string).

## Constants & flags
- mode `iRam002a6598 == 2` — rich-measure enable.
- flag `iRam002a6628` — 0 = range fast path, else table path.
- glyph thresholds `0x10`, `0x80`; advances 1 (half) / 2 (full).

## Corrected reconstruction
```c
int measure_text_width(void *str)
{
    if (iRam002a6598 != 2)
        return FUN_0010a050();          // fallback plain width

    int width = 0;
    void *cursor = str;
    long g;
    while ((g = FUN_001ade00(&cursor)) != 0) {     // next glyph, 0 = end
        if (iRam002a6628 == 0) {
            if (g < 0x10)      { width += 2; continue; }   // control/full-width
            if (g < 0x80)      { width += 1; continue; }   // ASCII half-width
            /* g >= 0x80: fall through to table path */
        }
        // table path (flag set, or wide glyph)
        u64 key = FUN_001adf30((void*)0x2a6590, g);
        long m  = FUN_001ae1a0((void*)0x2a6590, key);
        width += (m == 0) ? 2 : 1;      // 0 => full-width (2), else half (1)
    }
    return width;
}
```

## Evidence
- Threshold structure (`<0x10`, `<0x80`, `>=0x80`) plus a font-table fallback and a mode gate is the standard half/full-width string-metrics idiom.
- Iterator `FUN_001ade00(&cursor)` returning 0 at end; table lookups keyed by `0x2a6590`. Runtime-unvalidated.

## Notes / uncertainty
- Whether width units are pixels or cells is unconfirmed (treated as half-width cells). `transport` tag appears incorrect.
- `FUN_001ade00`'s decode (raw byte vs SJIS lead/trail) not fully traced.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001ae3e0.c`  — untouched decompiler output.
