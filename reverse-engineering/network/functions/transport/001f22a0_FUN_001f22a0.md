# 0x001f22a0 FUN_001f22a0

| field | value |
|---|---|
| Original address | 0x001f22a0 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_001f22a0 |
| Resolved name | iopdev_rpc_enumerate |
| Subsystem | transport |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** FUN_001f1fe0 (iopdev_rpc_init)
**Callees:** WaitSema, FUN_00115c68 (sceSifCallRpc), FUN_00114ac8 (debug print [inferred])
**Referenced globals:** 0x24e430 (DAT_0024e430 init-guard/sema), 0x37e9e0 (SifRpcClientData), 0x37ea40 (RPC send buffer), 0x37ea80 (RPC recv buffer), 0x37e9d0 (pending-op = 1), 0x37e9d8 (saved result ptr)
**Referenced strings:** 0x25a8a8 (RPC-error format)
**Referenced opcodes:** RPC fno 1 (enumerate/query)
**State vars:** DAT_0024e430, 0x37e9d0, 0x37e9d8

## Behavioral explanation
Issues the **enumerate/status RPC (fno 1)** on the device-RPC client 0x37e9e0. Gated on the subsystem
being initialized (`DAT_0024e430 != -1`). Takes the subsystem semaphore, calls `sceSifCallRpc` in
**async mode 1** (send 0x37ea40/0x10, recv 0x37ea80/0x90, completion cb 0x1f2868), and records the
pending op (0x37e9d0 = 1) plus the caller's result pointer (0x37e9d8 = param_1) so the completion
handler / poll can deliver the reply. Returns 0 on dispatch, -2 on RPC error, -1 if uninitialized.
This is the call `iopdev_rpc_init` uses to fetch the device count (recv size 0x90 carries the reply).

## Input / output
- `param_1` — result pointer stashed at 0x37e9d8 for the async completion to fill (device count/list).
- **Returns** `s64`: `0` on successful dispatch, `-2` (0xfffffffffffffffe) on sceSifCallRpc error,
  `-1` (0xffffffffffffffff) if the subsystem is uninitialized.

## Side effects
- WaitSema(implicit DAT_0024e430) before the RPC.
- `sceSifCallRpc(client 0x37e9e0, fno 1, mode 1, 0x37ea40/0x10, 0x37ea80/0x90, cb 0x1f2868)`.
- On success: `uRam0037e9d0 = 1` (pending op), `uRam0037e9d8 = param_1`.
- On error: `FUN_00114ac8(0x25a8a8, rc)` debug print.

## Important branches
- `DAT_0024e430 == -1` -> return -1 (not initialized; no sema, no RPC).
- `sceSifCallRpc != 0` -> print 0x25a8a8, return -2.
- else -> set pending state, return 0.

## Constants & flags
- RPC fno `1`; mode `1` = **async / SIF_RPCM_NOWAIT** [inferred].
- Send `0x10`, recv `0x90`. Completion cb `0x1f2868`. Pending-op code `1` @0x37e9d0.

## Corrected reconstruction
```c
// Dispatch the async "enumerate/status" RPC (fno 1); result delivered later to *param_1.
int64_t iopdev_rpc_enumerate(void *result_out) {                 // [inferred name]
    if (g_iopdev_sema == -1) return -1;                          // DAT_0024e430
    WaitSema(g_iopdev_sema);
    int rc = sceSifCallRpc(&g_iopdev_client, /*fno*/1, /*mode*/1, // FUN_00115c68
                           (void*)0x37ea40, 0x10,
                           (void*)0x37ea80, 0x90,
                           /*end_cb*/(void*)0x1f2868, /*arg*/0);
    if (rc != 0) { debug_print(0x25a8a8, rc); return -2; }       // FUN_00114ac8
    g_iopdev_pending_op = 1;          // 0x37e9d0
    g_iopdev_result_ptr = result_out; // 0x37e9d8
    return 0;
}
```

## Evidence
- Raw: guard `DAT_0024e430 != -1` / WaitSema / `FUN_00115c68(0x37e9e0,1,1,0x37ea40,0x10,0x37ea80,0x90,0x1f2868)`
  / on 0: set 0x37e9d0=1, 0x37e9d8=param_1 / else print 0x25a8a8, return -2.
- `FUN_00115c68` = sceSifCallRpc (SUBSYSTEM_MAP.md:312 — same primitive behind snap_ipc_call_rpc).
- Runtime-unvalidated.

## Remaining uncertainty
- Whether the RPC is truly async-mode 1 vs a mode flag is inferred from the arg and the separate poll
  (0x1f27c8). The completion cb 0x1f2868 and result marshalling into *param_1 not traced here. Medium.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_001f22a0.c`  — untouched decompiler output.
