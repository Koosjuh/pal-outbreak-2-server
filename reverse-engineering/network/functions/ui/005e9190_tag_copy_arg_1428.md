# 0x005e9190 tag_read_str_1428

| field | value |
|---|---|
| Original address | 0x005e9190 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_005e9190 |
| Resolved name | tag_read_str_1428 |
| Subsystem | ui |
| Relevance | unrelated |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** — (address-taken; dispatched from the markup tag table)
**Callees:** FUN_005ec1b0 (attribute-value extractor)
**Referenced globals:** markup-ctx base `iRam007012a0`; field `+0x1428` (0x100-byte string)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** ctx+0x1428

## Behavioral explanation
Markup attribute handler. Extracts the current tag's attribute value into the 256-byte
string field `ctx+0x1428` via the shared extractor `FUN_005ec1b0`. This string is passed
(alongside `ctx+0x1328`, see `tag_read_str_1328`) into the anchor-draw routine
`FUN_005ee470 → FUN_005d9790` — i.e. one of the two text/label strings drawn for a
highlighted link region.

## Input / output
- Input: `param_1` = `char**` parser cursor pointer.
- Output: `undefined8` 0 (consumed).

## Side effects
- Writes up to 256 bytes (incl NUL) to `ctx+0x1428`; advances `*param_1`.

## Important branches
None here (branching inside `FUN_005ec1b0`).

## Constants & flags
- `0x100` = destination capacity (extractor stops at `len-3`, NUL-terminates).

## Corrected reconstruction
```c
/* +0x1428 : 256-byte text/label string, drawn by FUN_005ee470/FUN_005d9790 */
undefined8 tag_read_str_1428(char **cursor)
{
    parse_attr_value(cursor, (u8*)g_markup_ctx + 0x1428, 0x100);  /* FUN_005ec1b0 */
    return 0;
}
```

## Evidence
- Raw: `FUN_005ec1b0(param_1, iRam007012a0 + 0x1428, 0x100)`.
- Consumer: `FUN_005ee470` passes `iRam007012a0 + 0x1428` to `FUN_005d9790` (draw). Runtime-unvalidated.

## Remaining uncertainty
Field is confirmed a drawn string; whether it is label vs URL-like target is inferred from draw-arg position.

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_005e9190.c`  — untouched decompiler output.
