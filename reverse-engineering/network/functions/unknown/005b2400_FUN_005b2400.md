# 0x005b2400 const_false_stub

| field | value |
|---|---|
| Original address | 0x005b2400 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_005b2400 |
| Resolved name | const_false_stub |
| Subsystem | unknown |
| Relevance | helper |
| Status | reconstructed |
| Confidence | Low |
| Runtime validated | no |

**Callers:** FUN_005f3e30
**Callees:** —
**Referenced globals:** —
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Constant stub: takes nothing and always returns 0. Its single caller (0x5f3e30) uses the return as a predicate/branch condition, so this reads as a **disabled / always-false gate** — a feature hook that is compiled in but hard-wired off (e.g. a debug or region-gated capability that is never enabled in this build). Body-wise there is nothing to reconstruct beyond the constant.

## Input / output
- `void`.
- returns `undefined8` — always `0`.

## Side effects
None.

## Important branches
None (single `return 0`).

## Constants & flags
- Return value fixed at `0`.

## Corrected reconstruction
```c
// Always-false predicate. Present but disabled at this build's link time;
// caller 0x5f3e30 treats a 0 return as "feature off".
u64 const_false_stub(void)
{
    return 0;
}
```

## Evidence
- Raw `FUN_005b2400.c`: entire body is `return 0;` (size=8, one caller).
- Runtime-unvalidated; semantic role is inferred from the caller's use of the return as a gate.

## Remaining uncertainty
- What feature this gate would enable if non-zero is unknown — needs analysis of caller 0x5f3e30's branch. It may be a linker-patched stub whose real implementation lives in another build/overlay.

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_005b2400.c`  — untouched decompiler output.
