# 0x005f52e0 FUN_005f52e0

| field | value |
|---|---|
| Original address | 0x005f52e0 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_005f52e0 |
| Resolved name | FUN_005f52e0 (opaque — softkbd_reset_tail [inferred]) |
| Subsystem | unknown |
| Relevance | unrelated |
| Status | documented |
| Confidence | Unknown |
| Runtime validated | no |

**Callers:** FUN_005f4b70 (softkbd_reset)
**Callees:** —
**Referenced globals:** —
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
**Opaque.** Ghidra decodes this 32-byte function to `halt_baddata()` — the disassembler hit bad/misaligned
instruction data at entry and truncated control flow, so nothing is recoverable from the decompile. The only
context is its single caller `softkbd_reset` (0x5f4b70), which clears the soft-keyboard active byte and then
tail-calls here; that makes this **most likely the remainder of the soft-keyboard reset** (e.g. a buffer
memset or widget teardown) whose bytes the overlay disassembly could not align. Not classifiable from code.

## Input / output
- Unknown (declared `void(void)` by Ghidra, but the prototype is unreliable given the bad decode).

## Side effects
- Unknown.

## Important branches
- None recoverable.

## Constants & flags
- None recoverable.

## Corrected reconstruction
Not reconstructable — the function body is non-decodable (`halt_baddata`). Low-level bytes were not recovered
by the decompiler. Preserving as opaque rather than inventing behavior.
```
FUN_005f52e0:   ; 32 bytes, Ghidra: "Bad instruction - Truncating control flow"
                halt_baddata()   ; disassembly misaligned; real body unknown
```

## Evidence
- Raw decompile emits only `halt_baddata()` with the "bad instruction data" warning.
- Sole caller is `softkbd_reset`; context suggests soft-kbd teardown but this is inference, not evidence.
- Runtime-unvalidated and un-decodable.

## Remaining uncertainty
- Entire behavior. A corrected disassembly (fixed overlay base / instruction alignment in Ghidra) or a
  runtime single-step is required to recover it.

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_005f52e0.c`  — untouched decompiler output.
