# 0x00614750 FUN_00614750 (thunk/alignment region)

| field | value |
|---|---|
| Original address | 0x00614750 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_00614750 |
| Resolved name | FUN_00614750 |
| Subsystem | unknown |
| Relevance | helper |
| Status | documented |
| Confidence | Low |
| Runtime validated | no |

**Callers:** (Ghidra lists FUN_0060f910, FUN_00613c30, FUN_00614b90 — see note: these actually target the adjacent EXT thunks at 0x614770/0x614780)
**Callees:** —
**Referenced globals:** —
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Not real recoverable code. Ghidra decodes `0x00614750` as a `halt_baddata()` stub — the disassembler hit bytes it can't interpret as a valid instruction and truncated control flow. The 32-byte window at 0x614750 is a **thunk / alignment region**: immediately after it sit two external-call thunks — `thunk_EXT_FUN_001af0a0` @ 0x614770 (the viewport clip push/pop used by 0x613c30 and 0x614b90) and `thunk_EXT_FUN_001af020` @ 0x614780. The "callers" Ghidra attributes to 0x614750 are really references to those adjacent thunks; 0x614750 itself is padding / a jump-island landing, not a called function.

## Input / output
- Undetermined — no valid prologue decoded.

## Side effects / branches / constants
- None recoverable. `halt_baddata` is a decoder sentinel, not an emitted instruction.

## Corrected reconstruction
Opaque — preserved as raw. No corrected C block is asserted (inventing one would fabricate behavior).
```
0x00614750:  <bad/aligned bytes>  ; halt_baddata (decoder truncation)
0x00614770:  thunk_EXT_FUN_001af0a0   ; clip/viewport push-pop  (real, called)
0x00614780:  thunk_EXT_FUN_001af020   ; ext call thunk          (real, called)
```

## Evidence
- Raw: `FUN_00614750.c` — `/* WARNING: Bad instruction - Truncating control flow here */ halt_baddata();`, followed by the two `thunk_EXT_FUN_*` blocks in the same listing.
- `thunk_EXT_FUN_001af0a0` is the clip primitive already reconstructed as the push/pop bracket in 0x613c30.
- Confidence **Low** (region is not decodable as a function); runtime-unvalidated.

## Remaining uncertainty
Whether these bytes are pure alignment padding, a compiler-emitted branch island, or a mis-split of the following thunks. Resolving requires re-disassembling the 0x614750–0x614790 span with correct instruction boundaries (out of scope for a source-only pass).

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_00614750.c`  — untouched decompiler output.
