# 0x00616768 FUN_00616768

| field | value |
|---|---|
| Original address | 0x00616768 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_00616768 |
| Resolved name | FUN_00616768 (unrecovered_pic_trampoline) |
| Subsystem | unknown |
| Relevance | helper |
| Status | documented |
| Confidence | Low |
| Runtime validated | no |

**Callers:** — (none in this overlay's call graph)
**Callees:** — (none recovered)
**Referenced globals:** —
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Not reconstructable from the decompiler output. Ghidra recovered only a `halt_baddata()` and 18
"Possible PIC construction … Changing call to branch" fragments at a regular ~0x20-byte stride
(`0x616784, 0x6167a4, 0x6167c4, … 0x6169a4`), every one of which is then marked as an unreachable/removed
block. There is no live control flow, no arguments, no callees, and no callers. This is the classic
signature of a **position-independent jump/branch table or a PLT-style dispatch trampoline** that the
disassembler failed to linearize (each ~0x20-byte slot is one `mov $t9, target; jr $t9`-style stub).

## Input / output
Unknown — no recoverable body.

## Side effects
Unknown.

## Important branches
The 18 uniform PIC-branch slots at 0x20 stride strongly imply a **dispatch table with ~18 entries**
(indexed jump), but the index source and the branch targets are not recovered.

## Constants & flags
- 18 PIC-construction stubs, ~0x20-byte spacing across `0x00616768`–`0x006169ac`.
- `halt_baddata` — Ghidra sentinel for "bad instruction, truncating control flow", not a real call.

## Corrected reconstruction
Not attempted — the function is opaque at the decompile level. Preserving the low-level observation
rather than inventing behavior:
```
0x00616768: <PIC jump-table / trampoline, 18 x ~0x20-byte branch stubs>
            Ghidra emits halt_baddata(); all blocks unreachable.
            Requires raw MIPS disassembly (Ghidra "disassemble" + manual jump-table
            reconstruction) to recover the 18 branch targets and the index register.
```

## Evidence
- Raw: `sources/overlays/overlay-3dat-decompile/FUN_00616768.c` (size 620). 18 "Possible PIC construction
  … Changing call to branch" warnings + 18 "Removing unreachable block" warnings + terminal `halt_baddata`.
- No callers/callees in the overlay call graph.

## Remaining uncertainty
Everything. The number of table entries (~18) and its trampoline nature are the only inferences.
Status held at **documented** / Low — no corrected pseudocode is possible without re-disassembling the
raw bytes and rebuilding the jump table. Recommend a `snap-re` raw-disasm pass on `0x00616768`–`0x006169ac`.
