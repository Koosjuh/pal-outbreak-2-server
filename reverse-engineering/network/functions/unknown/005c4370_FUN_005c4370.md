# 0x005c4370 FUN_005c4370

| field | value |
|---|---|
| Original address | 0x005c4370 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_005c4370 |
| Resolved name | FUN_005c4370 |
| Subsystem | unknown |
| Relevance | unrelated |
| Status | documented |
| Confidence | Low |
| Runtime validated | no |

**Callers:** FUN_0062cc60
**Callees:** —
**Referenced globals:** —
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Not a recoverable function. Ghidra decoded the bytes at 0x005c4370 as an invalid instruction and truncated control flow (`halt_baddata()`), size 12 bytes. This indicates the address is either misaligned disassembly, data misclassified as code, or the tail of an adjacent function. No behavior can be reconstructed without a corrected disassembly of the surrounding region.

## Input / output
- Unknown — the byte region does not decode to a valid MIPS function body.

## Side effects
- Unknown.

## Important branches
- None recoverable (`halt_baddata` at entry).

## Constants & flags
- None recoverable.

## Corrected reconstruction
Not attempted — inventing a body would violate the no-guessing rule. Preserving the low-level state:
```c
/* 0x005c4370: 12 bytes decode to an invalid instruction.
 * Ghidra: "Bad instruction - Truncating control flow here" -> halt_baddata().
 * Likely misaligned code or data-as-code; needs manual re-disassembly of the
 * region around the single caller FUN_0062cc60 to recover the true target. */
```

## Evidence
- Raw decompile: `WARNING: Bad instruction - Truncating control flow here; halt_baddata();`
- Single caller FUN_0062cc60 (outside the network set).
- Confidence **Low**.

## Remaining uncertainty
- Everything: entry point validity, arity, and behavior are all unknown. Re-run disassembly with corrected alignment before attempting reconstruction.

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_005c4370.c`  — untouched decompiler output.
