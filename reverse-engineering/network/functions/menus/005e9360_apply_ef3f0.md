# 0x005e9360 commit_run_ef3f0

| field | value |
|---|---|
| Original address | 0x005e9360 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_005e9360 |
| Resolved name | commit_run_ef3f0 |
| Subsystem | menus |
| Relevance | support |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** FUN_005e9270 (`tag_line_break_reset`)
**Callees:** FUN_005ef3f0 (commit/draw current run)
**Referenced globals:** — (delegates)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Pure trampoline: forwards to `FUN_005ef3f0`, the run-commit routine. `FUN_005ef3f0`
snapshots the run's style byte (`ctx+0x17c`), resets the glyph counter `ctx+0x124=0`,
seeds `ctx+0x168[0]=3`, resolves the named attributes `+0x2a3`→`ctx+0x128[]`,
`+0x193`→`ctx+0x120`, `+0x2b3`→`ctx+0x178` via `FUN_005ec4f0`, then (when draw-enabled
flags `+0x186`/`+0xe96b` are clear) issues the draw through `FUN_005d8a70` +
`FUN_005d95f0(ctx+0x1a3)`. This wrapper exists so the line-break tag can be an
address-taken handler of uniform signature.

## Input / output
- Input: none (void).
- Output: `undefined8` 0.

## Side effects
- All effects are inside `FUN_005ef3f0` (glyph-run commit + conditional draw).

## Important branches
None locally; the draw gate (`+0x186==0 && +0xe96b==0`) is in `FUN_005ef3f0`.

## Constants & flags
- None locally.

## Corrected reconstruction
```c
/* Trampoline to the run-commit/draw routine. */
undefined8 commit_run_ef3f0(void)
{
    commit_current_run();   /* FUN_005ef3f0 */
    return 0;
}
```

## Evidence
- Raw: `FUN_005ef3f0(); return 0;`.
- `FUN_005ef3f0` body confirms the commit pipeline (counter reset, id resolution, draw gate).
  Runtime-unvalidated.

## Remaining uncertainty
None on this wrapper; downstream detail lives in `FUN_005ef3f0`'s own record.

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_005e9360.c`  — untouched decompiler output.
