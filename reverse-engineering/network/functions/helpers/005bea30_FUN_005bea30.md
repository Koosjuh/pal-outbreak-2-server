# 0x005bea30 FUN_005bea30

| field | value |
|---|---|
| Original address | 0x005bea30 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_005bea30 |
| Resolved name | msgbuilder_slot_bea30 (inert slot) |
| Subsystem | helpers |
| Relevance | helper |
| Status | reconstructed |
| Confidence | Low |
| Runtime validated | no |

**Callers:** FUN_005c0dc0, FUN_005c1e70, FUN_005c1f30, FUN_005c1ff0
**Callees:** —
**Referenced globals:** 0x7018d0 (builder object, arg1 — by family convention)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Method slot of the 0x7018d0 builder family. Called from four builder wrappers in the 0x5c1xxx
cluster (`FUN_005c0dc0`, `FUN_005c1e70`, `FUN_005c1f30`, `FUN_005c1ff0`). The compiled body is
an 8-byte leaf: writes nothing, returns void — inert in this build. Family position implies a
builder field/commit primitive; specific role not recoverable from an empty body.

## Input / output
- **Params:** `arg1` = builder object (`0x7018d0`, by family convention); further args unknown.
- **Returns:** void.

## Side effects
None (leaf, no stores).

## Important branches
None.

## Constants & flags
None.

## Corrected reconstruction
```c
// Inert builder method slot. Behavior confirmed empty; specific role unknown.
void msgbuilder_slot_bea30(void *builder_obj /*=0x7018d0*/ /*, ... */)
{
    return;   // jr ra ; nop
}
```

## Evidence
- Raw decompile: 8-byte leaf, `return;`.
- Four callers, all in the 0x5c0-0x5c1 builder-wrapper cluster.
- Behavior (no-op) certain; role Unknown. Runtime-unvalidated.

## Remaining uncertainty
Specific builder operation — opaque from an empty body.

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_005bea30.c`  — untouched decompiler output.
