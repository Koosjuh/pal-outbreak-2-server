# 0x005bea80 FUN_005bea80

| field | value |
|---|---|
| Original address | 0x005bea80 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_005bea80 |
| Resolved name | msgbuilder_end_record (inert slot) |
| Subsystem | helpers |
| Relevance | helper |
| Status | reconstructed |
| Confidence | Low |
| Runtime validated | no |

**Callers:** FUN_005bda80, FUN_005bdeb0, FUN_005bdef0, FUN_005beba0, FUN_005bec80, FUN_005becf0, FUN_005bed60, FUN_005bedd0, FUN_005bee40, FUN_005bee90, FUN_005bef00, FUN_005c08b0, FUN_005c0dc0, FUN_005c1e70, FUN_005c1f30, FUN_005c1ff0, FUN_005c2080, FUN_005c2250, FUN_005c27d0, FUN_005c4460, FUN_005c6fb0
**Callees:** —
**Referenced globals:** 0x7018d0 (builder object, arg1)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
The record-finalizer method slot of the 0x7018d0 builder family and the trailing call in every
register/menu wrapper (21 callers), always `FUN_005bea80(0x7018d0)` after the commit slot
`FUN_005bea10`. Positionally it "ends the current record / finalizes the builder entry". The
compiled body is an 8-byte leaf: writes nothing, returns void — inert in this build.

## Input / output
- **Params:** `arg1` = builder object (`0x7018d0`).
- **Returns:** void.

## Side effects
None (leaf, no stores).

## Important branches
None.

## Constants & flags
None.

## Corrected reconstruction
```c
// "End record / finalize" slot on the 0x7018d0 builder. Inert body in this build.
// [inferred] role from fixed terminal position in every builder wrapper (after commit bea10).
void msgbuilder_end_record(void *builder_obj /*=0x7018d0*/)
{
    return;   // jr ra ; nop
}
```

## Evidence
- Raw decompile: 8-byte leaf, `return;`.
- Uniform terminal callsite: `...; FUN_005bea10(0x7018d0); FUN_005bea80(0x7018d0);` across all
  register wrappers (bdeb0, beba0) and menu builders (bec80/becf0/bed60).
- Behavior (no-op) certain; role inferred from position across 21 callers. Runtime-unvalidated.

## Remaining uncertainty
Whether the intended finalize writes builder state (overridden elsewhere). Inert here.

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_005bea80.c`  — untouched decompiler output.
