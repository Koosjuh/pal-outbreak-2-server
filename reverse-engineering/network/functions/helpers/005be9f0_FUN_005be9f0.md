# 0x005be9f0 FUN_005be9f0

| field | value |
|---|---|
| Original address | 0x005be9f0 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_005be9f0 |
| Resolved name | msgbuilder_set_msgtype (inert slot) |
| Subsystem | helpers |
| Relevance | helper |
| Status | reconstructed |
| Confidence | Low |
| Runtime validated | no |

**Callers:** FUN_00606780
**Callees:** —
**Referenced globals:** 0x7018d0 (builder object, arg1)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Method slot of the 0x7018d0 builder family. Called once from `FUN_00606780` as
`FUN_005be9f0(0x7018d0, 0x1031)` right after that function confirms a received message whose
16-bit type field (`CONCAT11(uRam007018c2,uRam007018c3)`) equals `0x1031`. Positionally it
"sets/echoes the message type (0x1031)" on the builder before the reply is assembled and sent
via `func_0x001ee590`. The compiled body is an 8-byte leaf that returns nothing and writes
nothing — inert in this build.

## Input / output
- **Params:** `arg1` = builder object (`0x7018d0`); `arg2` = message type (`0x1031`).
- **Returns:** void.

## Side effects
None (leaf, no stores).

## Important branches
None.

## Constants & flags
`0x1031` — the message type this reply path handles (matched against the inbound header at the
callsite). Its 8-bit sub-fields `bRam007018c6/c7` are copied to `bRam007018da/db` by the caller.

## Corrected reconstruction
```c
// Message-type setter slot for the 0x7018d0 reply builder. Inert body in this build.
// [inferred] name from callsite: invoked with the just-matched inbound type 0x1031.
void msgbuilder_set_msgtype(void *builder_obj /*=0x7018d0*/, uint16_t msg_type /*=0x1031*/)
{
    return;   // jr ra ; nop
}
```

## Evidence
- Raw decompile: 8-byte leaf, `return;`.
- Caller `FUN_00606780`: reads inbound via `FUN_00605d60(...,0x7018c0,...)`, checks
  `CONCAT11(uRam007018c2,uRam007018c3)==0x1031`, then calls this + `FUN_005bea00(0x7018d0,2)` +
  `FUN_005bea50(0x7018d0,buf,10)` + `FUN_005bea10(0x7018d0)` and sends the assembled buffer.
- Behavior (no-op) certain; slot intent inferred from the single callsite. Runtime-unvalidated.

## Remaining uncertainty
Designed effect (write msg_type into builder header) is not present here; inferred from position.

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_005be9f0.c`  — untouched decompiler output.
