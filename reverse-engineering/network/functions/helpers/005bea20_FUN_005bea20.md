# 0x005bea20 FUN_005bea20

| field | value |
|---|---|
| Original address | 0x005bea20 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_005bea20 |
| Resolved name | msgbuilder_slot_bea20 (inert slot) |
| Subsystem | helpers |
| Relevance | helper |
| Status | reconstructed |
| Confidence | Low |
| Runtime validated | no |

**Callers:** FUN_005bda80, FUN_005bee40
**Callees:** —
**Referenced globals:** 0x7018d0 (builder object, arg1 — by family convention)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Method slot of the 0x7018d0 builder family (contiguous 8-byte leaves at 0x5be9d0..0x5bea80).
Called from two register-style wrappers (`FUN_005bda80`, `FUN_005bee40`). The compiled body is
an 8-byte leaf that writes nothing and returns void — inert in this build. Its family position
(between the setters and finalizers) makes it a builder field/commit primitive, but the specific
role is not recoverable from an empty body.

## Input / output
- **Params:** `arg1` = builder object (`0x7018d0`, by family convention); further args unknown.
- **Returns:** void.

## Side effects
None (leaf, no stores).

## Important branches
None.

## Constants & flags
None.

## Corrected reconstruction
```c
// Inert builder method slot. Behavior confirmed empty; specific role unknown.
void msgbuilder_slot_bea20(void *builder_obj /*=0x7018d0*/ /*, ... */)
{
    return;   // jr ra ; nop
}
```

## Evidence
- Raw decompile: 8-byte leaf, `return;`.
- Callers `FUN_005bda80`, `FUN_005bee40` — both members of the register/wrapper cluster that
  drives the 0x7018d0 builder family.
- Behavior (no-op) certain; role Unknown. Runtime-unvalidated.

## Remaining uncertainty
Specific builder operation (arg count and semantics) — opaque from an empty body.

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_005bea20.c`  — untouched decompiler output.
