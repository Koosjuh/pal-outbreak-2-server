# 0x00608730 select_byte_6c5035_or_36

| field | value |
|---|---|
| Original address | 0x00608730 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_00608730 |
| Resolved name | select_byte_6c5035_or_36 |
| Subsystem | helpers |
| Relevance | helper |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** FUN_005f4480
**Callees:** —
**Referenced globals:** 0x006c5034 flag; 0x006c5035 alt-value; 0x006c5036 default-value
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** cRam006c5034

## Behavioral explanation
A branch-select accessor over three adjacent option bytes. Returns the default byte `0x006c5036`
normally; if the flag byte `0x006c5034` is non-zero it returns the alternate byte `0x006c5035`
instead. Reads like "if override-flag set, use override value, else use base value" — a settings
read used by the options/settings handler `FUN_005f4480`.

## Input / output
- **Params:** none.
- **Return:** `undefined1` (byte) — the selected value.

## Side effects
- None (pure read).

## Important branches
- `g_flag_6c5034 != 0` → return `g_alt_6c5035` (override path).
- else → return `g_default_6c5036` (base path).

## Constants & flags
- `0x006c5034` — boolean-style selector flag (`!= 0` toggles the override).

## Corrected reconstruction
```c
extern uint8_t g_flag_6c5034;    // 0x006c5034  override flag
extern uint8_t g_alt_6c5035;     // 0x006c5035  value used when flag set
extern uint8_t g_default_6c5036; // 0x006c5036  value used when flag clear

uint8_t select_option_byte(void)
{
    uint8_t v = g_default_6c5036;
    if (g_flag_6c5034 != 0)
        v = g_alt_6c5035;
    return v;
}
```

## Evidence
- Raw decompile `FUN_00608730.c`: `uVar1 = uRam006c5036; if (cRam006c5034 != '\0') uVar1 = uRam006c5035; return uVar1;`.
- Single caller `FUN_005f4480` (options/settings handler). Runtime-unvalidated.

## Remaining uncertainty
- The specific option these three bytes encode is not identified from this function alone. Semantics
  (which setting) unproven; the select logic itself is certain.

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_00608730.c`  — untouched decompiler output.
