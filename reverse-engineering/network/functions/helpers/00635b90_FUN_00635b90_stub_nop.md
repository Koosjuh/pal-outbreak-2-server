# 0x00635b90 kbd_stub_nop

| field | value |
|---|---|
| Original address | 0x00635b90 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_00635b90 |
| Resolved name | kbd_stub_nop |
| Subsystem | helpers |
| Relevance | helper |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** FUN_006300a0
**Callees:** —
**Referenced globals:** —
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
An 8-byte no-op function: the body is a bare `return`. It has a single caller (`FUN_006300a0`, in the same keyboard-widget cluster). In the original C this is almost certainly an empty callback / hook slot in the widget's dispatch or lifecycle table — for example a per-mode "on-open" or "on-tick" handler that this keyboard variant does not implement, so the function pointer resolves to an intentional empty function rather than NULL (calling a real no-op avoids a null-check at the call site).

## Input / output
- **Params:** none observed (declared `void`; the MIPS prologue takes no arguments it uses).
- **Returns:** void. No value, no side effect.

## Side effects
None. Touches no globals, emits no packet, changes no state.

## Important branches
None — straight-line `return`.

## Constants & flags
None.

## Corrected reconstruction
```c
// Empty widget callback slot (a hook this keyboard mode does not implement).
// Present so the dispatch table entry is a valid, callable no-op.
void kbd_stub_nop(void)
{
    /* intentionally empty */
}
```

## Evidence
- Raw decompile: size=8, single `return;`, zero callees, one caller (`FUN_006300a0`).
- Cluster membership (caller is in the 0x63xxxx keyboard-widget range) supports "unimplemented hook", not dead code.
- Runtime-unvalidated (static only).

## Remaining uncertainty
- Whether it is genuinely a table hook vs. a compiler-emitted stub for an inlined/removed body cannot be proven statically. Intent is [inferred]; confidence capped at Medium because the function carries no evidence of its own purpose.

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_00635b90.c`  — untouched decompiler output.
