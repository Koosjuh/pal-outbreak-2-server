# 0x005bea70 FUN_005bea70

| field | value |
|---|---|
| Original address | 0x005bea70 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_005bea70 |
| Resolved name | msgbuilder_append_string (inert slot) |
| Subsystem | helpers |
| Relevance | helper |
| Status | reconstructed |
| Confidence | Low |
| Runtime validated | no |

**Callers:** FUN_005bec80, FUN_005becf0, FUN_005bed60, FUN_005bedd0, FUN_005bee90
**Callees:** —
**Referenced globals:** 0x7018d0 (builder object, arg1)
**Referenced strings:** label strings passed by callers (e.g. 0x6cbe4a, 0x6cbe8b, 0x6cbe96)
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
The string-append method slot of the 0x7018d0 builder family. Every menu-label builder calls it
as `FUN_005bea70(0x7018d0, <str_ptr>, <len>)`, where `<len>` is the result of
`func_0x0010a050(<str_ptr>)` (a strlen). It "appends a counted string field (ptr,len)" to the
menu record. The compiled body is an 8-byte leaf: writes nothing, returns void — inert in this
build (the observable label text must be materialised elsewhere).

## Input / output
- **Params:** `arg1` = builder object (`0x7018d0`); `arg2` = string pointer; `arg3` = string
  length (from strlen at callsite).
- **Returns:** void.

## Side effects
None (leaf, no stores).

## Important branches
None.

## Constants & flags
None in this function. Callers supply label pointers 0x6cbe4a (0xaf), 0x6cbe8b (0xb0), 0x6cbe96
(0xb1), etc.

## Corrected reconstruction
```c
// Counted-string append slot on the 0x7018d0 menu builder. Inert body in this build.
// [inferred] name/role from callsites: append (str, strlen(str)) as a menu label field.
void msgbuilder_append_string(void *builder_obj /*=0x7018d0*/, const char *str, int len)
{
    return;   // jr ra ; nop
}
```

## Evidence
- Raw decompile: 8-byte leaf, `return;`.
- Uniform callsite: `uVar1 = func_0x0010a050(strptr); FUN_005bea70(0x7018d0, strptr, uVar1);`
  in bec80/becf0/bed60 (menu labels 0xaf/0xb0/0xb1) and bedd0/bee90.
- Behavior (no-op) certain; role inferred from the (ptr,strlen) callsite pattern. Runtime-unvalidated.

## Remaining uncertainty
Whether the intended append writes into the builder (overridden elsewhere). Inert here.

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_005bea70.c`  — untouched decompiler output.
