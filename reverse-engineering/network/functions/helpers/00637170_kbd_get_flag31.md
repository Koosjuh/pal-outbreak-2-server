# 0x00637170 kbd_get_flag31

| field | value |
|---|---|
| Original address | 0x00637170 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_00637170 |
| Resolved name | kbd_get_flag31 |
| Subsystem | helpers |
| Relevance | helper |
| Status | classified (record from evidence-based classification; corrected pseudocode pending for non-core) |
| Confidence | High |
| Runtime validated | no |

**Callers:** FUN_005b1d20, FUN_005f4be0, FUN_00618c90, FUN_00619800, FUN_00637180
**Callees:** —
**Referenced globals:** 0x715da8 state struct
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** 0x31 flag

## Behavioral explanation
Getter: returns byte at struct+0x31 (a request/dirty flag consumed by the widget's close/refresh path).

## Notes / uncertainty
Public getter returning ctx+0x31 result/state byte, polled per-frame by host UI screens. Value encoding (open/confirm/cancel) inferred; writers of +0x31 not traced.

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_00637170.c`  — untouched decompiler output.

## Evidence
- Classified from the raw decompile via the fan-out pass (callers/callees/globals/strings/control-flow).
- Confidence **High**. Runtime validation pending. Promote to `reconstructed`/`validated` with corrected pseudocode + a trace.
