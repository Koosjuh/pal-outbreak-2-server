# 0x005bea00 FUN_005bea00

| field | value |
|---|---|
| Original address | 0x005bea00 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_005bea00 |
| Resolved name | msgbuilder_set_field2 (inert slot) |
| Subsystem | helpers |
| Relevance | helper |
| Status | reconstructed |
| Confidence | Low |
| Runtime validated | no |

**Callers:** FUN_00606780
**Callees:** —
**Referenced globals:** 0x7018d0 (builder object, arg1)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Method slot of the 0x7018d0 builder family. Called once from `FUN_00606780` as
`FUN_005bea00(0x7018d0, 2)`, immediately after the message-type is set (`FUN_005be9f0(...,0x1031)`)
and before the payload bytes are appended. Positionally it "sets a small scalar builder field to
2" (a subtype / field-count / version). The compiled body is an 8-byte leaf: no writes, returns
void — inert in this build.

## Input / output
- **Params:** `arg1` = builder object (`0x7018d0`); `arg2` = scalar value (`2`).
- **Returns:** void.

## Side effects
None (leaf, no stores).

## Important branches
None.

## Constants & flags
`2` — literal scalar the reply path sets on the builder for the 0x1031 message.

## Corrected reconstruction
```c
// Small-scalar setter slot on the 0x7018d0 reply builder. Inert body in this build.
// [inferred] name/role: sets a subtype/count field to 2 for the 0x1031 reply.
void msgbuilder_set_field2(void *builder_obj /*=0x7018d0*/, int value /*=2*/)
{
    return;   // jr ra ; nop
}
```

## Evidence
- Raw decompile: 8-byte leaf, `return;`.
- Caller `FUN_00606780`: sequence `set_msgtype(0x1031)` → `bea00(0x7018d0,2)` → copy sub-bytes →
  `bea50(0x7018d0,buf,10)` → `bea10(0x7018d0)` → assemble+send.
- Behavior (no-op) certain; role inferred from the single callsite. Runtime-unvalidated.

## Remaining uncertainty
Designed meaning of the `2` (subtype vs field-count vs version) — not resolvable from an inert body.

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_005bea00.c`  — untouched decompiler output.
