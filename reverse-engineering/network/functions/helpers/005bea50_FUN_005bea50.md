# 0x005bea50 FUN_005bea50

| field | value |
|---|---|
| Original address | 0x005bea50 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_005bea50 |
| Resolved name | msgbuilder_append_bytes (inert slot) |
| Subsystem | helpers |
| Relevance | helper |
| Status | reconstructed |
| Confidence | Low |
| Runtime validated | no |

**Callers:** FUN_00606780
**Callees:** —
**Referenced globals:** 0x7018d0 (builder object, arg1)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Method slot of the 0x7018d0 builder family. Called once from the reply assembler `FUN_00606780`
as `FUN_005bea50(0x7018d0, buf, 10)`, where `buf` is a 16-byte stack scratch previously filled by
`func_0x001ca6b0(buf, 0x8710f1, ...)`. Positionally it "appends a fixed 10-byte field" to the
0x1031 reply record before the commit/finalize. The compiled body is an 8-byte leaf: writes
nothing, returns void — inert in this build.

## Input / output
- **Params:** `arg1` = builder object (`0x7018d0`); `arg2` = source buffer pointer;
  `arg3` = length (`10`).
- **Returns:** void.

## Side effects
None (leaf, no stores).

## Important branches
None.

## Constants & flags
`10` — byte length of the appended field in the 0x1031 reply.

## Corrected reconstruction
```c
// Fixed-length byte-append slot on the 0x7018d0 reply builder. Inert body in this build.
// [inferred] name/role from callsite: append 10 bytes from a formatted scratch buffer.
void msgbuilder_append_bytes(void *builder_obj /*=0x7018d0*/, const void *src, int len /*=10*/)
{
    return;   // jr ra ; nop
}
```

## Evidence
- Raw decompile: 8-byte leaf, `return;`.
- Caller `FUN_00606780`: `func_0x001ca6b0(auStack_30,0x8710f1,...)` then
  `FUN_005bea50(0x7018d0,auStack_30,10)` in the 0x1031 reply-build sequence.
- Behavior (no-op) certain; role inferred from the single callsite. Runtime-unvalidated.

## Remaining uncertainty
Whether the intended append writes into the builder buffer (overridden elsewhere). Inert here.

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_005bea50.c`  — untouched decompiler output.
