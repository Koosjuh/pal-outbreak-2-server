# 0x005be9d0 FUN_005be9d0

| field | value |
|---|---|
| Original address | 0x005be9d0 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_005be9d0 |
| Resolved name | msgbuilder_hook_noop (inert pre-send hook) |
| Subsystem | helpers |
| Relevance | helper |
| Status | reconstructed |
| Confidence | Low |
| Runtime validated | no |

**Callers:** FUN_005bdfe0
**Callees:** —
**Referenced globals:** —
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Inert hook. The compiled body is an 8-byte MIPS leaf (`jr ra` + delay-slot `nop`) that does
nothing and returns. It is fired exactly once from `FUN_005bdfe0` on the path that emits a
`0xFF` status byte while the module is in transport state `2` (see caller context), immediately
before the 1-byte send helper `FUN_005be120`. In this PAL build the hook carries no observable
side effect — it is a placeholder/override slot, not a data path.

## Input / output
- **Params:** none (called `FUN_005be9d0()` with no arguments).
- **Returns:** void.

## Side effects
None. No global, struct, packet, or event is touched — the function body cannot contain a store
(8 bytes total).

## Important branches
None. Straight-line return.

## Constants & flags
None in this function. Context constant at the callsite: status byte `0xFF` (negative/error
status) emitted in state `sRam006cbc7e == 2` when `cRam006cbc84 != 0`.

## Corrected reconstruction
```c
// Inert override/hook slot. Behavior confirmed empty from the 8-byte leaf body.
// Purpose [inferred]: a "before-negative-status-send" notification the PAL build leaves stubbed.
void msgbuilder_hook_noop(void)
{
    return;   // jr ra ; nop
}
```

## Evidence
- Raw decompile: 8-byte function, single `return;` (leaf, no stores possible).
- Caller `FUN_005bdfe0` (state machine on `sRam006cbc7e`): in state `2`, `cRam006cbc84 != 0`
  path sets `auStack_10[0]=0xff`, calls this hook, then `FUN_005be120(buf,1,0)` (1-byte send).
- Behavior (returns, no effect) is certain; the *intent* of the slot is inferred. Runtime-unvalidated.

## Remaining uncertainty
Whether this slot is meant to be overridden in another build/overlay. Its inert behavior here is
certain; its designed purpose is not.

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_005be9d0.c`  — untouched decompiler output.
