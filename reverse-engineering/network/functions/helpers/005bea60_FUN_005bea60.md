# 0x005bea60 FUN_005bea60

| field | value |
|---|---|
| Original address | 0x005bea60 |
| Binary / overlay | 3.DAT |
| Module | overlay-3dat |
| Original generated name | FUN_005bea60 |
| Resolved name | msgbuilder_slot_bea60 (inert slot) |
| Subsystem | helpers |
| Relevance | helper |
| Status | reconstructed |
| Confidence | Low |
| Runtime validated | no |

**Callers:** FUN_005bdef0, FUN_005c1ff0, FUN_005c6fb0
**Callees:** —
**Referenced globals:** 0x7018d0 (builder object, arg1 — by family convention)
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Method slot of the 0x7018d0 builder family. Called from three builder wrappers
(`FUN_005bdef0`, `FUN_005c1ff0`, `FUN_005c6fb0`). The compiled body is an 8-byte leaf: writes
nothing, returns void — inert in this build. Family position implies a builder field/commit
primitive; specific role not recoverable from an empty body.

## Input / output
- **Params:** `arg1` = builder object (`0x7018d0`, by family convention); further args unknown.
- **Returns:** void.

## Side effects
None (leaf, no stores).

## Important branches
None.

## Constants & flags
None.

## Corrected reconstruction
```c
// Inert builder method slot. Behavior confirmed empty; specific role unknown.
void msgbuilder_slot_bea60(void *builder_obj /*=0x7018d0*/ /*, ... */)
{
    return;   // jr ra ; nop
}
```

## Evidence
- Raw decompile: 8-byte leaf, `return;`.
- Three callers across the register/builder-wrapper clusters (bdef0, c1ff0, c6fb0).
- Behavior (no-op) certain; role Unknown. Runtime-unvalidated.

## Remaining uncertainty
Specific builder operation — opaque from an empty body.

## Raw decompilation
`sources/overlays/overlay-3dat-decompile/FUN_005bea60.c`  — untouched decompiler output.
