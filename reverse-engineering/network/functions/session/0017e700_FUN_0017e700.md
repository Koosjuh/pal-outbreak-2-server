# 0x0017e700 FUN_0017e700

| field | value |
|---|---|
| Original address | 0x0017e700 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_0017e700 |
| Resolved name | dbcsock_close |
| Subsystem | session |
| Relevance | core |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** — (dbcman socket API surface)
**Callees:** FUN_0017f230 (dbcman_close, fn 0x302)
**Referenced globals:** 0x277c00 socket table (stride 0x334): +0x08 in-use, +0x0c bound, +0x14 bind-handle
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Closes a sceDbcMan socket slot. It issues the IOP close RPC (`dbcman_close`, fn 0x302) and, on
success, clears the slot's state fields — in-use (`+0x08`), bound (`+0x0c`), and bind-handle (`+0x14`)
— returning the slot to the free pool. Returns 1 on success, or the (negative) RPC result on failure.

## Input / output
- `param_1` (`int`) — socket-table index to close.
- Returns `long`: `1` on success; the negative `dbcman_close` result if the RPC failed.

## Side effects
- `dbcman_close()` (RPC fn 0x302).
- On success: `sock_table[param_1].in_use = 0` (`+0x277c08`), `.bound = 0` (`+0x277c0c`),
  `.bind_handle = 0` (`+0x277c14`).

## Important branches
- `dbcman_close() < 0` -> return that value; slot fields left intact.
- `>= 0` -> clear slot fields, return 1.

## Constants & flags
- Socket table base `0x277c00`, stride `0x334`; cleared fields `+0x08`, `+0x0c`, `+0x14`.

## Corrected reconstruction
```c
long dbcsock_close(int idx)                  // param_1
{
    long r = dbcman_close(/*handle from slot*/);   // FUN_0017f230, fn 0x302
    if (r >= 0) {
        g_sock[idx].in_use      = 0;         // +0x08
        g_sock[idx].bound       = 0;         // +0x0c
        g_sock[idx].bind_handle = 0;         // +0x14
        r = 1;
    }
    return r;
}
```

## Evidence
- Raw lines 7-14: `lVar1 = FUN_0017f230()`; `if (-1 < lVar1)` -> `param_1 *= 0x334`; clear
  `*(param_1+0x277c08)=0`, `*(+0x277c0c)=0`, `*(+0x277c14)=0`; `lVar1 = 1`.

## Remaining uncertainty
- The decompile calls `FUN_0017f230()` with no explicit argument (the handle is read from the shared
  RPC scratch / slot in practice); the exact handle source at the call site is not shown here.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_0017e700.c`  — untouched decompiler output.
