# 0x0017f4f8 FUN_0017f4f8

| field | value |
|---|---|
| Original address | 0x0017f4f8 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_0017f4f8 |
| Resolved name | dbcman_ctl_0x315 |
| Subsystem | session |
| Relevance | core |
| Status | reconstructed |
| Confidence | Medium |
| Runtime validated | no |

**Callers:** — (not in this batch's call graph)
**Callees:** FUN_00115c68 (sif_rpc_call), FUN_0017fd00 (log)
**Referenced globals:** 0x27b000 (arg), 0x27b004 (reply)
**Referenced strings:** 0x258ad8 (rpc-error log)
**Referenced opcodes:** —
**State vars:** SIF fn 0x80001315

## Behavioral explanation
Synchronous sceDbcMan control RPC **function 0x80001315** on the main channel. Sends one word
`param_1` at scratch `+0x00` and returns the IOP reply at `+0x04`. Structurally identical to
`dbcman_close`/`dbcman_config` but a different (higher-numbered) control op; the exact semantic is not
pinned. Note: unlike open/close/bind it is **not** init-gated in the decompile.

## Input / output
- `param_1` (`undefined4`) — control argument (endpoint handle or option selector).
- Returns `undefined4` = `uRam0027b004` on success; `0` on RPC failure.

## Side effects
- `uRam0027b000 = param_1`; `sif_rpc_call(0x27af88, 0x80001315, sync, …)`.

## Important branches
- `sif_rpc_call < 0` -> log `0x258ad8`, return 0.
- else -> return `uRam0027b004`.

## Constants & flags
- `0x80001315` = dbcman control fn (0x315 block: get-status / abort / option — unresolved).

## Corrected reconstruction
```c
#define DBC_FN_CTL_315 0x80001315u

u32 dbcman_ctl_0x315(u32 arg)                // param_1
{
    g_dbc_rpc.w0 = arg;                       // uRam0027b000
    long r = sif_rpc_call(&dbc_ctl_cd, DBC_FN_CTL_315, /*sync*/0,
                          g_dbc_rpc.buf, 0x280, g_dbc_rpc.buf, 0x280, 0);
    if (r < 0) { log("dbcman ctl(0x315) rpc error"); return 0; }  // 0x258ad8
    return g_dbc_rpc.w1;                      // uRam0027b004
}
```

## Evidence
- Raw lines 8-15: `uRam0027b000=param_1`; `FUN_00115c68(0x27af88, 0x80001315, …)`; return
  `uRam0027b004`; error `0x258ad8`. No `ram0x0022f590` gate present.

## Remaining uncertainty
- No caller in this set; semantic role of fn 0x315 unresolved (kept as `dbcman_ctl_0x315`). Whether
  the arg is a handle or an option code is unknown.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_0017f4f8.c`  — untouched decompiler output.
