# 0x0017eb98 FUN_0017eb98

| field | value |
|---|---|
| Original address | 0x0017eb98 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_0017eb98 |
| Resolved name | dbcsock_bind |
| Subsystem | session |
| Relevance | core |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** FUN_0017e900 (dbcsock_poll)
**Callees:** FUN_0017f2b8 (dbcman_bind, fn 0x303)
**Referenced globals:** 0x277c00 socket table (stride 0x334): +0x0c bound flag, +0x14 bind-handle
**Referenced strings:** —
**Referenced opcodes:** —
**State vars:** —

## Behavioral explanation
Binds a socket slot: calls `dbcman_bind` (fn 0x303, which first checks the IOP link/open flag for the
index) and, on success, stores the returned bind handle into the slot (`+0x14`) and sets the slot's
bound flag (`+0x0c = 1`). Returns the `dbcman_bind` result (bind handle on success, negative/error on
failure).

## Input / output
- `param_1` (`int`) — socket-table index.
- Returns `long`: bind handle (`>= 0`) on success; negative error if `dbcman_bind` failed (e.g.
  `0xfffffff4` when the endpoint flag isn't 1).

## Side effects
- On success: `sock_table[param_1].bind_handle = result` (`+0x277c14`), `.bound = 1` (`+0x277c0c`).

## Important branches
- `dbcman_bind() < 0` -> return the error; slot unchanged.
- `>= 0` -> store handle, set bound flag, return handle.

## Constants & flags
- Socket table base `0x277c00`, stride `0x334`; `+0x0c` bound, `+0x14` handle.

## Corrected reconstruction
```c
long dbcsock_bind(int idx)                   // param_1
{
    long r = dbcman_bind(idx);               // FUN_0017f2b8, fn 0x303
    if (r >= 0) {
        g_sock[idx].bind_handle = (u32)r;    // +0x14
        g_sock[idx].bound       = 1;         // +0x0c
    }
    return r;
}
```

## Evidence
- Raw lines 7-12: `lVar1 = FUN_0017f2b8()`; `if (-1 < lVar1)` -> `*(param_1*0x334 + 0x277c14) = lVar1`;
  `*(param_1*0x334 + 0x277c0c) = 1`.

## Remaining uncertainty
- None material for the slot bookkeeping; the error semantics come entirely from `dbcman_bind`
  (0x0017f2b8).

## Raw decompilation
`sources/executables/netstack-decompile/FUN_0017eb98.c`  — untouched decompiler output.
