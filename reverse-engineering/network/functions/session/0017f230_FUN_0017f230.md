# 0x0017f230 FUN_0017f230

| field | value |
|---|---|
| Original address | 0x0017f230 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_0017f230 |
| Resolved name | dbcman_close |
| Subsystem | session |
| Relevance | core |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** FUN_0017e700 (dbcsock_close)
**Callees:** FUN_00115c68 (sif_rpc_call), FUN_0017fd00 (log)
**Referenced globals:** ram0x0022f590 (init gate), 0x27b000 (arg), 0x27b004 (reply)
**Referenced strings:** 0x258a98 (rpc-error log)
**Referenced opcodes:** —
**State vars:** SIF fn 0x80001302

## Behavioral explanation
Synchronous sceDbcMan **close-endpoint RPC, function 0x80001302**. Sends the endpoint handle
`param_1` in scratch `+0x00` and returns the IOP status word from `+0x04`. Init-gated.

## Input / output
- `param_1` (`undefined4`) — endpoint handle to close (the socket slot's stored handle).
- Returns `undefined4` = `uRam0027b004` (IOP status) on success; `0` if uninitialised or RPC failure.

## Side effects
- `uRam0027b000 = param_1`; `sif_rpc_call(0x27af88, 0x80001302, sync, …)`.

## Important branches
- `ram0x0022f590 == 0` -> return 0.
- `sif_rpc_call < 0` -> log `0x258a98`, return 0.
- else -> return `uRam0027b004`.

## Constants & flags
- `0x80001302` = dbcman fn CLOSE.

## Corrected reconstruction
```c
#define DBC_FN_CLOSE 0x80001302u

u32 dbcman_close(u32 handle)                 // param_1
{
    if (!g_dbc_inited) return 0;             // ram0x0022f590
    g_dbc_rpc.w0 = handle;                    // uRam0027b000
    long r = sif_rpc_call(&dbc_ctl_cd, DBC_FN_CLOSE, /*sync*/0,
                          g_dbc_rpc.buf, 0x280, g_dbc_rpc.buf, 0x280, 0);
    if (r < 0) { log("dbcman close rpc error"); return 0; }   // 0x258a98
    return g_dbc_rpc.w1;                      // uRam0027b004
}
```

## Evidence
- Raw lines 10-22: init gate; `uRam0027b000=param_1`; `FUN_00115c68(0x27af88,0x80001302,…)`; return
  `uRam0027b004`; error path logs `0x258a98`.

## Remaining uncertainty
- The decompiler shows a `param_1` that the caller (dbcsock_close) invokes with no argument — the
  handle is read from the socket slot in practice. Meaning of the reply word (freed/notfound) unconfirmed.

## Raw decompilation
`sources/executables/netstack-decompile/FUN_0017f230.c`  — untouched decompiler output.
