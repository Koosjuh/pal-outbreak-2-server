# 0x0017fd28 FUN_0017fd28

| field | value |
|---|---|
| Original address | 0x0017fd28 |
| Binary / overlay | SLES_533.19 |
| Module | main-ee |
| Original generated name | FUN_0017fd28 |
| Resolved name | dbc_cmd_get_01038002 |
| Subsystem | session |
| Relevance | core |
| Status | reconstructed |
| Confidence | High |
| Runtime validated | no |

**Callers:** — (none in the network callgraph subset; reached via the DBC/session-layer client, likely by pointer/higher layer)
**Callees:** `FUN_0017fc10` (generic DBC "get" RPC — `dbc_rpc_get`)
**Referenced globals:** none directly (callee touches `iRam0027b000/b004/b008/b20c`, RPC client `0x27af88`)
**Referenced strings:** none directly (callee logs via `FUN_0017fd00`)
**Referenced opcodes:** DBC command `0x01038002` (embedded in the DBC packet header, distinct from SN@P app opcodes)
**State vars:** none

## Behavioral explanation
A one-command specialization of the generic DBC receive/query stub `FUN_0017fc10`
(`dbc_rpc_get`). It issues DBC command **`0x01038002`** to the IOP-side `dbcman`
module over `sceSifCallRpc` (function `0x8000131a`, client-data block `0x27af88`,
fixed 0x280-byte transfer) and delivers the response bytes into the caller's
buffer. On success it returns the **number of bytes received**; on RPC/transport
failure it propagates the negative error code.

This is part of the lower DBC transport under the SN@P/session layer, not a SN@P
app opcode handler. `0x01038002` is a DBC sub-command (a status/data query); its
exact semantic is not established from this stub alone.

## Input / output
- `param_1` (`dbc_handle_t`) — DBC device/connection handle, forwarded verbatim as the RPC `param_1` (stored to `uRam0027b000`). [inferred role]
- `param_2` (`void *out_buf`) — destination buffer; the callee copies `iRam0027b008` (out-length) bytes into it. [inferred]
- **Return** (`int`):
  - `>= 0` → byte count actually received (`aiStack_20[0]` == callee's out-length).
  - `< 0` → error result from `FUN_0017fc10` (negative RPC/`iRam0027b20c`).

## Side effects
- No globals written in this frame. Via the callee: sets `uRam0027b000=handle`,
  `uRam0027b004=0x01038002`, `iRam0027b008=*len` (request length), fires the IOP RPC,
  and on success copies the reply payload out of `0x27b00c` into `out_buf`.
- No SN@P packet, no room/member state transition. Pure transport request/response.

## Important branches
- `iVar1 = FUN_0017fc10(...)` then **`if (-1 < iVar1)`**:
  - true (RPC ok, `iRam0027b20c >= 0`) → overwrite return with `aiStack_20[0]` (received length).
  - false (negative) → return the raw negative error unchanged.
- (Inside callee, for context) RPC `sceSifCallRpc` failure → callee logs via `FUN_0017fd00(0x258c38)` and returns 0; a negative `iRam0027b20c` is passed straight through here.

## Constants & flags
- `DBC_CMD_GET_0x01038002` — DBC get/query sub-command placed in the packet header (`uRam0027b004`). Evidence: literal 2nd arg to `FUN_0017fc10`.
- (callee) `DBC_RPC_FN_GET = 0x8000131a`, `DBC_RPC_CLIENT_GET = 0x27af88`, RPC buffer size `0x280`. Evidence: `FUN_0017fc10` → `FUN_00115c68`.

## Corrected reconstruction
```c
// DBC receive/query wrapper: issue DBC command 0x01038002 over the generic IOP
// RPC "get" path (dbc_rpc_get / FUN_0017fc10) and hand the reply back to caller.
// Returns bytes received (>=0) or a negative RPC/transport error.
// [inferred] handle/buffer param roles taken from generic callee usage.
int dbc_cmd_get_01038002(dbc_handle_t handle, void *out_buf)
{
    int io_len[4];   // io_len[0]: in = requested length (LEFT UNINITIALIZED here),
                     //            out = bytes the IOP actually returned
    int rc;

    rc = dbc_rpc_get(handle, DBC_CMD_GET_0x01038002, io_len, out_buf); // FUN_0017fc10
    if (rc >= 0)          // RPC succeeded (iRam0027b20c >= 0)
        rc = io_len[0];   // -> return actual received byte count copied into out_buf
    return rc;            // < 0 => RPC/transport error, propagated
}

enum { DBC_CMD_GET_0x01038002 = (int)0x01038002 };
```

## Evidence
- `FUN_0017fd28.c` L8: `FUN_0017fc10(param_1,0xffffffff01038002,aiStack_20,param_2)` — command literal + in/out-length slot + destination buffer.
- L9–11: `if (-1 < iVar1) iVar1 = aiStack_20[0];` — success remaps return to received length.
- Callee `FUN_0017fc10.c`: sets `uRam0027b000/b004/b008`, calls `FUN_00115c68(0x27af88,0xffffffff8000131a,...,0x280,...)` (sceSifCallRpc), copies `iRam0027b008` bytes from `0x27b00c` into `param_4`, returns `iRam0027b20c`.
- Sibling family confirms a DBC IOP module: send-side strings `dbcman__SendData2_BUSY`, `sceDbcSendData2__rpc_error` in `FUN_0017f948`.
- Runtime-unvalidated.

## Remaining uncertainty
- `io_len[0]` is passed to the RPC **uninitialized** (request length is stack garbage). Likely the `0x01038002` op ignores the request length (fixed 0x280 transfer), but this is unconfirmed — could be a decompiler artifact of an untracked init.
- Domain meaning of DBC command `0x01038002` (which status/data it fetches) is unknown from this stub.
- `param_1` handle semantics and `out_buf` size/content unconfirmed (no runtime trace, no callers in subset).

## Raw decompilation
`sources/executables/netstack-decompile/FUN_0017fd28.c`  — untouched decompiler output.
